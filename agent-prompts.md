# Prompt สำหรับ Agent — Phase ตาม `goal.md`

> สร้างเมื่อ 2026-09-11 · อ้างอิงแผนใน [`goal.md`](./goal.md)

> ⚠️ **เดิมวางไว้ 4 ตัว แต่กลายเป็น 5** — หลังแบ่งงาน 4 ตัวเสร็จ มีฟอร์มใหม่เพิ่มเข้ามา
> (G10 แบบสอบถามนักศึกษา) ซึ่งเป็นฟอร์มใหม่ทั้งฟอร์ม มี route ใหม่ wizard ใหม่ schema ใหม่
> **ยัดเข้า agent ตัวไหนก็ทำให้ตัวนั้นใหญ่เกินและเสี่ยงพัง** จึงแยกเป็น Agent 5
> ถ้าต้องการ 4 ตัวจริง ๆ ให้ตัด Agent 4 (คู่มือ) ออกไปทำทีหลัง เพราะยังไงก็ต้องรอ UI นิ่งก่อนอยู่แล้ว

---

## วิธีแบ่งงาน และทำไมแบ่งแบบนี้

งานใน `goal.md` แบ่งเป็น G1–G9 แต่ **ห้ามแบ่งให้ agent ตาม G** เพราะ G2/G5/G6 แย่งแก้ไฟล์ชุดเดียวกันทั้งหมด (`EvaluationWizard.tsx`, `steps.tsx`, `copy.ts`, `evaluation-schema.ts`) ถ้าแบ่งตาม G จะชนกันเละ

**แบ่งตาม "ความเป็นเจ้าของไฟล์" แทน** — แต่ละ agent เป็นเจ้าของไฟล์ชุดหนึ่งแบบผูกขาด ไม่มีไฟล์ไหนมี 2 เจ้าของ

### ลำดับการรัน

```
ขั้นที่ 1 (รันเดี่ยว ห้ามขนาน)
  └─ Agent 1 — Foundation
       วาง step constants ที่ Agent 2 และ 3 ต้องใช้ต่อ
       ถ้ารันขนานกับตัวอื่นจะชนแน่นอน

ขั้นที่ 2 (รันขนานได้ทั้ง 3 ตัว)
  ├─ Agent 2 — ฝั่งสถานประกอบการ
  ├─ Agent 3 — ฝั่งอาจารย์นิเทศ
  └─ Agent 5 — ฟอร์มนักศึกษา (ฟอร์มใหม่ทั้งฟอร์ม)

ตลอดทาง (ขนานได้ตั้งแต่ต้น)
  └─ Agent 4 — คู่มือ
       เขียนข้อความก่อนได้เลย แต่ถ่ายภาพหน้าจอหลัง Agent 2+3+5 เสร็จ
```

> **อัปเดต 2026-09-11:** ย้ายงานสร้าง `lib/coop-center-copy.ts` จาก Agent 2 ไป **Agent 1**
> เดิม Agent 5 ต้องรอ Agent 2 เพราะต้องใช้โมดูลนี้ พอย้ายไปขั้นที่ 1 แล้ว **Agent 2/3/5 ขนานได้หมด**
> ประหยัดเวลาไปหนึ่งรอบเต็ม ๆ

### ตารางความเป็นเจ้าของไฟล์

| Agent | ไฟล์ที่เป็นเจ้าของ (แก้ได้) | งานใน goal.md |
|---|---|---|
| 1 | `RatingCard.tsx`, **`lib/coop-center-copy.ts` (สร้างใหม่)**, ไฟล์ที่มีชื่อฟอร์ม/metadata, ส่วน step-constant ของทั้ง 2 wizard, `CONTEXT.md` | G1, G3, เฟสเตรียม |
| 2 | `components/evaluation/*`, `lib/evaluation-schema.ts`, `programs/[programId]/*` | G2, G5, G6, G4 (ฝั่ง company) |
| 3 | `components/advisor/*`, `lib/advisor-schema.ts`, `[school]/[program]/advisor/*`, `migrations/*` | G9, G4 (ฝั่ง advisor), migration |
| 4 | `docs/manual/*` เท่านั้น | G8 |
| 5 | `components/student/*`, `lib/student-schema.ts`, `[school]/[program]/student/*` (ทั้งหมดเป็นไฟล์ใหม่) | G10 |

⚠️ `RatingCard.tsx` เป็นของ Agent 1 แต่ Agent 2 ต้องใช้ prop ใหม่ — Agent 1 ต้องเพิ่ม prop ให้เสร็จในขั้นที่ 1

---

## กติกาที่ agent ทุกตัวต้องทำตาม (แปะไว้ทุก prompt)

```
กติกาบังคับของโปรเจกต์นี้ — ห้ามละเมิด:

1. ห้าม gate การเปลี่ยนหน้าด้วย validation
   goToStep / handleNext ต้องให้ผู้ใช้กดข้ามส่วนได้อิสระ ไม่มี required check
   validate เฉพาะใน handleSubmit ตอนกดส่งจริงเท่านั้น
   (กติกานี้เคยพังมาแล้วครั้งหนึ่ง อ่าน web/CLAUDE.md)

2. แยกภาษาเด็ดขาด
   ค่าใน th ห้ามมีอักษรละติน ค่าใน en ห้ามมีอักษรไทย
   รวมถึงข้อความที่ดึงจาก DB

3. ใช้ design token เดิม ห้ามเพิ่มสีใหม่
   โปรเจกต์ใช้สีแบรนด์เดียว (navy) อ่าน web/CLAUDE.md ส่วน Design System

4. ใช้ RatingCard ไม่ใช่ RatingScale
   RatingScale เก็บไว้เพื่อ backward compat เท่านั้น

5. ฟอร์มเป็น uncontrolled + มี draft restore
   คอมโพเนนต์ที่มี state ต้อง sync จาก DOM ผ่าน formVersion (useEffect)
   ไม่งั้นกู้ร่างแล้วค่าไม่ขึ้น — ดูแพตเทิร์นใน CompetencyStep

6. ห้ามเดาคำตอบของคำถามที่ยังค้างใน goal.md
   ใช้ค่าที่ผมเสนอไว้ในแผนเป็น default แล้วเขียน TODO กำกับไว้ในโค้ด
   พร้อมรายงานกลับมาว่าใช้ default ตัวไหนบ้าง

7. เสร็จแล้วต้องรัน `npm run build` และ `npm run lint` ใน web/ ให้ผ่าน
   ถ้าไม่ผ่าน ห้ามรายงานว่าเสร็จ

8. อย่าแตะไฟล์นอกรายการที่ระบุว่าเป็นของตัวเอง
   ถ้าจำเป็นต้องแตะ ให้หยุดแล้วรายงานกลับมาก่อน
```

---

# Agent 1 — Foundation (รันก่อน ห้ามขนาน)

```
โปรเจกต์: ระบบแบบประเมินผลลัพธ์การเรียนรู้รายวิชาสหกิจศึกษา (Next.js + Postgres)
ที่อยู่โค้ด: web/
อ่าน goal.md, CONTEXT.md และ web/CLAUDE.md ให้จบก่อนเริ่ม

คุณคือ Agent 1 งานของคุณคือวางรากฐานให้ Agent 2 และ 3 ทำงานต่อได้
Agent อื่นรอคุณอยู่ ทำให้เสร็จและถูกต้อง สำคัญกว่าทำเร็ว

=== งานที่ 1: เฟสเตรียม — เปลี่ยนเลข step เป็นค่าคงที่แบบมีชื่อ (สำคัญที่สุด) ===

ตอนนี้ทั้ง EvaluationWizard.tsx และ AdvisorWizard.tsx ฝังเลข step ดิบไว้หลายจุด:
  - hasMinimumData()      : step === 0 / 1 / 2 …
  - validateStep()        : step === 0 … 4
  - handleSubmit()        : if (i === 5) continue;
  - ส่วน render           : hidden={currentStep !== N} ทั้ง 6 บล็อก
  - fieldStep()           : map ชื่อฟิลด์ → เลข step

Phase นี้ทั้งสองฟอร์มจะสลับ/แทรกส่วนพร้อมกัน 4–5 ส่วน ถ้าปล่อยเป็นเลขดิบจะ
off-by-one แน่นอน

ให้ refactor เป็นค่าคงที่แบบมีชื่อ เช่น
  const COMPANY_STEP = { GENERAL: 0, SKILLS: 1, KNOWLEDGE: 2, ... } as const;
  const ADVISOR_STEP = { GENERAL: 0, KNOWLEDGE: 1, ... } as const;
แล้วแทนที่เลขดิบทุกจุดด้วยค่าคงที่

ข้อบังคับ: refactor นี้ต้อง "ไม่เปลี่ยนพฤติกรรมใด ๆ" ลำดับส่วนยังเป็นแบบเดิมทุกอย่าง
การสลับลำดับจริงเป็นงานของ Agent 2 และ 3 คุณแค่วางโครงให้เขา

=== งานที่ 2: G3 — rubric ตัดป้ายระดับออก ===

ไฟล์: web/src/components/evaluation/RatingCard.tsx

ปัจจุบันการ์ดแสดง 3 บรรทัด: เลข / ป้าย (ดีมาก, ดี, …) / คำอธิบาย rubric
ต้องการ: ถ้ามีคำอธิบาย → แสดงแค่ เลข + คำอธิบาย (ตัดป้ายออก)
         ถ้าไม่มีคำอธิบาย → แสดง เลข + ป้าย เหมือนเดิม (fallback)

ระวัง: ตรวจทั้ง description_th และ description_en — ถ้า th มีแต่ en ว่าง
โหมด en จะกลายเป็นการ์ดเปล่า ต้อง fallback เป็นป้าย en

=== งานที่ 3: เพิ่ม prop ให้ RatingCard รองรับระดับที่กดไม่ได้ ===

Agent 2 ต้องใช้ต่อ ถ้าคุณไม่ทำ เขาทำงานไม่ได้

เพิ่ม prop: disabledValues?: number[]
พฤติกรรม:
  - ระดับใน disabledValues ต้องกดไม่ได้ทั้งเมาส์และคีย์บอร์ด
  - ลูกศร ←/→ ต้อง "ข้าม" ระดับนั้นไป ไม่ใช่โฟกัสแล้วเลือกไม่ได้
    (ดู focusCard ใน RatingCard.tsx — ตอนนี้มันเลือกทันทีที่โฟกัส)
  - ต้องมี aria-disabled และสไตล์ที่เห็นชัดว่ากดไม่ได้
  - ไม่กระทบการใช้งานเดิมที่ไม่ส่ง prop นี้มา

=== งานที่ 3.5: สร้าง lib/coop-center-copy.ts (Agent 2, 3 และ 5 รอใช้) ===

คำถามประเมินศูนย์สหกิจศึกษาฯ 2 ข้อ จะไปปรากฏใน "ทั้ง 3 ฟอร์ม"
ถ้าปล่อยให้แต่ละ agent เก็บถ้อยคำของตัวเอง จะ drift แน่นอน
คุณต้องสร้างโมดูลกลางให้เสร็จในขั้นที่ 1 เพื่อให้อีก 3 ตัวขนานกันได้

ยึดถ้อยคำจาก components/advisor/advisor-copy.ts ที่ล็อกไว้แล้วเมื่อ 2026-09-07
(centerTitle + centerItems + rating4) เพราะเป็นชุดเดียวที่มีคำแปลอังกฤษที่ผ่านการตรวจแล้ว

ต้อง export อย่างน้อย:
  - ข้อความคำถาม 2 ข้อ แยก th/en
  - หัวข้อกลุ่ม แยก th/en (แต่ "ห้ามฝังเลขส่วนที่ N ลงในสตริง"
    เพราะแต่ละฟอร์มอยู่คนละส่วน — ให้ผู้เรียกประกอบเลขเอง)
  - ป้ายสเกล 4 ระดับ: 4 ดีเยี่ยม / 3 ดี / 2 พอใช้ / 1 ควรปรับปรุง แยก th/en

หมายเหตุ: ถ้อยคำในต้นฉบับฝั่งนักศึกษาต่างจากอีก 2 ฟอร์มเล็กน้อย
("การดำเนินการของศูนย์สหกิจศึกษา" vs "การดำเนินงานของศูนย์สหกิจศึกษาฯ")
ให้ยึดฉบับอาจารย์นิเทศเป็นมาตรฐานเดียว แล้วเขียน TODO กำกับ (Q14 ใน goal.md)

=== งานที่ 4: G1 — ชื่อแบบประเมินเต็ม ๆ ===

ชื่อที่ถูกต้อง:
  company : แบบประเมินผลการปฏิบัติสหกิจศึกษาจากสถานประกอบการ
  advisor : แบบประเมินนักศึกษาสหกิจศึกษาจากอาจารย์นิเทศ

ต้องเน้นคำว่า "สถานประกอบการ" และ "อาจารย์นิเทศ" ให้เห็นชัด
ห้ามย่อเหลือ "แบบประเมินหน่วยงาน" อีก

จุดที่ต้องแก้ (รายการเต็มอยู่ในตารางของ G1 ใน goal.md):
  app/[school]/[program]/[role]/[lang]/page.tsx  (~114 — ฝั่ง company ตอนนี้ไม่มีชื่อฟอร์มเลย)
  components/ui/form-picker-dialog.tsx
  components/ui/programs-list.tsx
  app/programs/[programId]/print/page.tsx
  app/programs/[programId]/export/docx/route.ts
  app/layout.tsx (metadata.title)
  components/evaluation/copy.ts (successTitle เท่านั้น — ห้ามแตะ rating/steps)
  components/advisor/AdvisorCompletionScreen.tsx (ข้อความ successTitle เท่านั้น)
  components/FormShell.tsx
  lib/i18n.ts
  CONTEXT.md (glossary)

⚠️ ข้อควรระวังเรื่องขอบเขต:
  - ใน copy.ts ให้แตะเฉพาะ successTitle — ตัวแปร rating / steps / reportItems
    เป็นของ Agent 2 ห้ามแตะเด็ดขาด
  - ใน AdvisorCompletionScreen ให้แตะเฉพาะข้อความ — ส่วนคำนวณคะแนนเป็นของ Agent 3

คำแปลอังกฤษยังไม่ได้รับอนุมัติ (Q1 ใน goal.md) ให้ใช้ค่าเสนอนี้ไปก่อนพร้อม TODO:
  company : Cooperative Education Performance Evaluation by the Workplace
  advisor : Cooperative Education Student Evaluation by the Faculty Advisor

=== เสร็จแล้วรายงาน ===
1. ชื่อค่าคงที่ step ที่ตั้งไว้ (Agent 2 และ 3 ต้องใช้ชื่อเดียวกัน)
2. signature ของ prop ใหม่ใน RatingCard
3. รายการ default ที่ใช้เพราะยังไม่ได้คำตอบ
4. ผล npm run build และ npm run lint
5. grep -rn "แบบประเมินหน่วยงาน" web/src ต้องไม่เจอผลลัพธ์
```

---

# Agent 2 — ฝั่งสถานประกอบการ (รันหลัง Agent 1)

```
โปรเจกต์: ระบบแบบประเมินผลลัพธ์การเรียนรู้รายวิชาสหกิจศึกษา (Next.js + Postgres)
อ่าน goal.md (G2, G4 ฝั่ง company, G5, G6), CONTEXT.md, web/CLAUDE.md ให้จบก่อนเริ่ม
Agent 1 วาง step constants และ prop disabledValues ของ RatingCard ไว้ให้แล้ว — ใช้ของเขา

ไฟล์ที่คุณเป็นเจ้าของ:
  web/src/components/evaluation/*
  web/src/lib/evaluation-schema.ts
  web/src/app/programs/[programId]/*
ห้ามแตะ components/advisor/*, lib/advisor-schema.ts, RatingCard.tsx

=== เป้าหมาย: ฝั่งสถานประกอบการเหลือ 6 ส่วนตามนี้ ===

  1 ข้อมูลทั่วไป                              (เดิม — ไม่เปลี่ยน)
  2 ทักษะที่สถานประกอบการคาดหวังจากนักศึกษา   (ใหม่ทั้งส่วน)
  3 ความรู้และทักษะ                           (เดิมส่วนที่ 2)
  4 จริยธรรมและบุคลิก                         (เดิมส่วนที่ 3)
  5 รายงาน/โครงงาน                            (เดิมส่วนที่ 4 — สเกลเปลี่ยน)
  6 ข้อเสนอแนะ                                (เดิมส่วนที่ 5 + เนื้อหาจากส่วนที่ 6 เดิม)

ส่วน "กระบวนการ" เดิม (ProcessStep) ถูกยุบทิ้งทั้งส่วน

=== งานที่ 1: G2 — ส่วนที่ 2 ใหม่ (งานใหญ่สุด) ===

สร้าง components/evaluation/SkillExpectationGroup.tsx

พฤติกรรม:
  - แสดงทักษะ 16 ข้อเป็น checkbox จัด 2 คอลัมน์บนจอกว้าง
  - บังคับเลือกอย่างน้อย 1 ข้อ ไม่จำกัดสูงสุด
  - ติ๊กข้อไหน → เด้งสเกลระดับความจำเป็น 1–5 ของข้อนั้นออกมา
  - สเกลแสดงครบ 5 ระดับ แต่ระดับ 1 ต้อง disabled กดไม่ได้
    (ใช้ prop disabledValues ที่ Agent 1 ทำไว้)
    เหตุผล: ติ๊กมาแล้วแปลว่าทักษะนั้นเกี่ยวข้อง จึงเลือก "ไม่จำเป็น" ไม่ได้
    ต้องมีข้อความอธิบายกำกับว่าทำไมกดไม่ได้ ไม่ใช่ปล่อยให้เทา ๆ เฉย ๆ
  - ติ๊กออก → ล้างค่าระดับของข้อนั้นทิ้ง ไม่ค้างไปกับ payload

รายการทักษะ 16 ข้อ (ไทย/อังกฤษ) และป้ายระดับ 1–5: ดูตารางเต็มใน G2 ของ goal.md
ชื่อฟิลด์: skill-{n} (checkbox) และ skill-{n}-level (radio, 2–5) โดย n = 1..16

สำคัญ: คะแนนส่วนนี้ "ไม่รวมในคะแนนนักศึกษา" เก็บเป็นข้อมูลเชิงสถิติเท่านั้น
ห้ามเข้า lo_score / lo_count / c_score และห้ามโผล่ในหน้าสรุปผลว่าเป็นคะแนนนักศึกษา

อย่าลืม: คอมโพเนนต์นี้มี state ต้อง sync จาก DOM ผ่าน formVersion
ดูแพตเทิร์นใน CompetencyStep ไม่งั้นกู้ร่างแล้วค่าไม่ขึ้น

=== งานที่ 2: G5 — ยุบส่วนกระบวนการ ย้ายเข้าส่วนข้อเสนอแนะ ===

ลบ ProcessStep ทิ้ง แล้วย้ายของเข้า FeedbackStep ต่อท้ายคำถาม next_year_count:

  ตัด  : expected_competencies ("สมรรถนะที่คาดหวังจากนักศึกษาสหกิจศึกษา") ลบทิ้ง
  แปลง : process_evaluation จากช่องข้อความปลายเปิด → คำถามให้คะแนน 2 ข้อ
  ย้าย : other_comments ไปต่อท้ายสุด

คำถามใหม่ 2 ข้อ หัวข้อกลุ่ม "ประเมินกระบวนการดำเนินงานของศูนย์สหกิจศึกษาและพัฒนาอาชีพ"
สเกล 4 ระดับ: 4 ดีเยี่ยม / 3 ดี / 2 พอใช้ / 1 ควรปรับปรุง
ข้อความเต็มทั้ง 2 ข้อ: ดู G5 ใน goal.md

⚠️ อย่าพิมพ์ข้อความใหม่เอง — คำถาม 2 ข้อนี้มีอยู่แล้วเป๊ะ ๆ ที่
components/advisor/advisor-copy.ts (centerTitle + centerItems) พร้อมคำแปลอังกฤษ
ที่ล็อกไว้แล้ว และ rating4 ก็ตรงกับที่ต้องการ

ให้ยกข้อความชุดนี้ขึ้นเป็นโมดูลกลาง web/src/lib/coop-center-copy.ts
แล้วให้ทั้งสองฟอร์ม import จากที่เดียวกัน ห้ามก๊อปวาง
(Agent 3 จะแก้ advisor-copy.ts ให้ import จากโมดูลนี้ — คุณสร้างโมดูล เขาต่อสาย
 ประสานกันผ่านรายงานสรุป)

คะแนน 2 ข้อนี้ไม่เข้า lo_score/c_score — เก็บแยกเป็น center_score / center_count

=== งานที่ 3: G4 (ฝั่ง company) — รายงานเหลือ 4 ระดับ ===

สเกลใหม่: 4 ดีมาก / 3 ดี / 2 พอใช้ / 1 ต้องปรับปรุง

⚠️ กับดักที่ต้องระวังที่สุดในงานนี้:
copy.ts มีตัวแปร `rating` ที่ถูกใช้ 2 ที่พร้อมกัน
  (ก) เป็นป้ายของส่วนรายงาน
  (ข) เป็น fallback ของคำถาม LO ที่ยังไม่มี options ใน DB
ถ้าแก้ตรง ๆ จะไปเปลี่ยนสเกล LO ของหลักสูตรที่ยังไม่ได้ seed โดยไม่ตั้งใจ
→ ต้องแยกเป็น reportRating คนละตัวก่อน แล้วค่อยแก้

จุดที่ต้องแก้:
  lib/evaluation-schema.ts   : reportItemSchema ช่วง 1..5 → 1..4
  components/evaluation/copy.ts : แยก reportRating ออกมา
  components/evaluation/steps.tsx : ReportStep ใช้ชุดใหม่
  app/programs/[programId]/actions.ts : guard c-* จาก > 5 เป็น > 4
  components/evaluation/CompletionScreen.tsx : hardcode `cCount * 5` → * 4
      (ถ้าลืมจุดนี้ หน้าขอบคุณจะโชว์ 16 / 25 ทั้งที่เต็ม 20)

=== งานที่ 4: G6 — ประกอบโครงสร้าง 6 ส่วน ===

ใช้ step constants ของ Agent 1 อย่าใช้เลขดิบ
แก้ให้ครบทุกจุด: hasMinimumData, validateStep, handleSubmit, บล็อก render,
fieldStep (ต้อง map skill-* ไปส่วนที่ถูกต้อง), copy.steps ทั้ง th และ en

ต้องมี guard สำหรับร่างเก่า: draft.currentStep ที่บันทึกไว้ด้วยโครงสร้างเดิม
จะชี้คนละส่วนแล้ว ห้ามให้เปิดมาแล้วเด้งผิดส่วนหรือพัง

=== เกณฑ์ผ่านที่ต้องทดสอบเองก่อนรายงาน ===
  - ยังไม่กรอกอะไรเลย กดข้ามส่วนได้ทุกส่วน ไม่มีอะไรบล็อก
  - กดส่งโดยไม่ติ๊กทักษะ → error และเด้งกลับส่วนที่ 2
  - ระดับ 1 ในส่วนที่ 2 กดไม่ได้ทั้งเมาส์และคีย์บอร์ด
  - ติ๊กทักษะออก → ค่าระดับหาย ไม่ค้างใน payload
  - บันทึกร่างแล้วรีเฟรช → checkbox และระดับกลับมาครบ
  - ส่วนรายงานแสดง 4 การ์ด เรียง 4→1
  - คำถาม LO ที่ไม่มี options ใน DB ยังเป็นสเกลเดิม ไม่ถูกเปลี่ยนโดยไม่ตั้งใจ
  - หน้าสรุปผลแสดงตัวหารรายงานเป็น 20 ไม่ใช่ 25
  - ไม่เหลือช่อง "สมรรถนะที่คาดหวังจากนักศึกษาสหกิจศึกษา"
  - โหมด th ไม่มีอักษรละติน โหมด en ไม่มีอักษรไทย
  - npm run build และ npm run lint ผ่าน

=== รายงานกลับ ===
path ของ lib/coop-center-copy.ts และ export ที่มีในนั้น (Agent 3 ต้องใช้)
+ รายการ default ที่ใช้เพราะยังไม่ได้คำตอบ + ผล build/lint
```

---

# Agent 3 — ฝั่งอาจารย์นิเทศ + migration (รันหลัง Agent 1 ขนานกับ Agent 2 ได้)

```
โปรเจกต์: ระบบแบบประเมินผลลัพธ์การเรียนรู้รายวิชาสหกิจศึกษา (Next.js + Postgres)
อ่าน goal.md (G9, G4 ฝั่ง advisor), CONTEXT.md, web/CLAUDE.md ให้จบก่อนเริ่ม
Agent 1 วาง step constants ไว้ให้แล้ว — ใช้ของเขา

ไฟล์ที่คุณเป็นเจ้าของ:
  web/src/components/advisor/*
  web/src/lib/advisor-schema.ts
  web/src/app/[school]/[program]/advisor/*
  migrations/*
ห้ามแตะ components/evaluation/*, lib/evaluation-schema.ts, RatingCard.tsx

=== งานที่ 1: G9 — จัดลำดับส่วนใหม่ทั้งฟอร์ม ===

โครงสร้างใหม่ (ยืนยันกับผู้ใช้แล้ว):

  ส่วนที่ 1  ข้อมูลทั่วไป                      (เดิมส่วนที่ 1 — ไม่เปลี่ยน)
  ส่วนที่ 2  ความรู้และทักษะ                    (เดิม "LO ชุดแรก" — เปลี่ยนแค่ชื่อ)
  ส่วนที่ 3  จริยธรรมและบุคลิก                  (เดิม "LO ชุดสอง" — เปลี่ยนแค่ชื่อ)
  ส่วนที่ 4  รายงาน/โครงงาน                     (ย้ายขึ้นมาจากส่วนที่ 6)
  ส่วนที่ 5  ข้อคิดเห็น                         (เดิมส่วนที่ 4 ชื่อ "อื่น ๆ")
  ส่วนที่ 6  ศูนย์สหกิจฯ + สถานประกอบการ        (เดิมส่วนที่ 5 + เพิ่มข้อ 6)

รายละเอียดย่อย G9.1–G9.5 อยู่ใน goal.md อ่านให้ครบ สรุปย่อ:

G9.1 ชื่อส่วนที่ 2/3 ต้องตรงกับฝั่งสถานประกอบการ
     "LO ชุดแรก"/"LO ชุดสอง" เป็นศัพท์ภายในที่คนนอกไม่เข้าใจ
     ให้ import ชื่อจาก WIZARD_COPY ที่เดียวกัน ไม่ใช่พิมพ์ซ้ำ

G9.2 ส่วนที่ 5 เปลี่ยนชื่อ "อื่น ๆ" → "ข้อคิดเห็น" / "Other" → "Comments"
     และ otherTitle: "ด้านอื่น ๆ" → "ข้อคิดเห็น"

G9.3 otherItems ตัดคำนำหน้า "ด้านที่ 6 ด้านอื่น ๆ: " ออกทั้ง 2 ข้อ
     ฝั่งอังกฤษตัด "Topic 6 Others: " ออกเช่นกัน
     ข้อความใหม่เต็ม ๆ ดูตารางใน G9.3

G9.4 เพิ่มข้อ 6 ในส่วนที่ 6 ต่อจากข้อ 5 "ความพึงพอใจต่อสถานประกอบการโดยภาพรวม"
     คำถาม: "6. ควรเป็นหน่วยงานพรีเมี่ยมหรือไม่" (บังคับตอบ)
     ไม่ใช่สเกล 4 ระดับ — เป็นตัวเลือก 3 ทาง: ใช่ / ไม่ใช่ / ทบทวนดูก่อน
     เลือก "ทบทวนดูก่อน" → ต้องระบุเหตุผล (ช่องข้อความบังคับกรอก)

     ⚠️ อย่าคิดโครงใหม่ — แพตเทิร์นนี้มีอยู่แล้วในไฟล์เดียวกัน
     ดู adv_future_placement + adv_future_placement_other ใน advisor-schema.ts
     ที่ใช้ superRefine บังคับกรอกช่อง "โปรดระบุ" เมื่อเลือก other
     และ UI ที่ advisor-steps-extra.tsx:243 ลอกโครงนั้นมาเลย

     ฟิลด์ใหม่: adv_premium_workplace, adv_premium_workplace_reason
     ไม่รวมในคะแนน — ห้ามเข้า workplaceScore

G9.5 แก้เลข "ส่วนที่ N" ที่ฝังอยู่ในข้อความหัวข้อ — จุดที่พังง่ายที่สุด
     advisor-copy.ts ฝังเลขไว้ในสตริงโดยตรง พอสลับลำดับแล้วเลขผิดทันที
       centerTitle    : ส่วนที่ 2 → ส่วนที่ 6
       workplaceTitle : ส่วนที่ 3 → ส่วนที่ 6
       reportTitle    : ส่วนที่ 4 → คงเดิม (บังเอิญตรงอยู่แล้ว)
       otherTitle     : เพิ่มเป็น ส่วนที่ 5
     ฝั่งอังกฤษต้องแก้คู่กันทุกบรรทัด (Part 2: → Part 6: ฯลฯ)

     ปัญหาค้าง (Q13 ใน goal.md): ส่วนที่ 6 มี 2 หัวข้อกลุ่มในหน้าเดียว
     จะได้ "ส่วนที่ 6" ซ้ำกัน 2 อัน
     ใช้ default นี้ไปก่อน: หัวข้อใหญ่ "ส่วนที่ 6" อันเดียว สองกลุ่มย่อยไม่ใส่เลข
     แล้วเขียน TODO กำกับ

     ข้อเสนอที่อยากให้ทำถ้าเวลาพอ: เลิกฝังเลขในสตริง ให้คอมโพเนนต์ประกอบเลข
     จาก ADVISOR_STEPS ตอน render แทน — ปัญหานี้จะเกิดซ้ำทุกครั้งที่สลับลำดับ

=== งานที่ 2: G4 (ฝั่ง advisor) — รายงานเหลือ 4 ระดับ ===

สเกลใหม่: 4 ดีมาก / 3 ดี / 2 พอใช้ / 1 ต้องปรับปรุง

  lib/advisor-schema.ts : advisorReportStepSchema ใช้ adv4ScoreSchema แทน adv5ScoreSchema
                          แล้ว "ลบ adv5ScoreSchema ทิ้ง" ไม่ใช่ปล่อยค้าง
  advisor-copy.ts       : ลบ rating5 ทิ้ง ให้ reportItems ใช้ rating4
  advisor/actions.ts    : guard ช่วงคะแนน adv-report-* และตัวหารของ reportScore
  AdvisorCompletionScreen.tsx : ตรวจตัวหารคะแนนรายงาน

⚠️ คอมเมนต์ในไฟล์พวกนี้เขียนล็อกไว้แข็งมากว่า "only the report is 5-level" และ
"this split is an explicit, locked project rule, not an oversight"
กติกานั้นถูกยกเลิกแล้ว — ต้องแก้คอมเมนต์ให้ตรงความจริงใหม่ ไม่งั้นคนอ่านทีหลังเข้าใจผิด
ตอนนี้คือ 4 ระดับทั้งหมด ไม่มีข้อยกเว้น

=== งานที่ 3: migration แปลงคะแนนรายงานเดิม ===

ก่อนอื่น "นับจำนวน submission จริงในระบบก่อน" ถ้าเป็น 0 ให้ข้ามงานนี้แล้วรายงานมา
(Q6 ใน goal.md — ประหยัดเวลาไปครึ่งงาน)

ถ้ามีข้อมูลจริง เขียน migrations/019_convert_report_scale_5_to_4.mjs
  สูตร: c_score_ใหม่ = round(c_score_เดิม / 25 * 20)   (คงเปอร์เซ็นต์เดิม)
  ต้องเพิ่มคอลัมน์เก็บค่าดิบเดิมไว้กู้คืนได้ (c_score_legacy หรือ scale_version)
  ก่อนเขียนทับ ห้ามทับทิ้งโดยไม่มีทางย้อน
  payload_json เก็บค่ารายข้อไว้เป็นหลักฐาน ไม่ต้องแปลง

ห้ามรัน migration กับฐานข้อมูลจริงเอง เขียนสคริปต์แล้วรายงานมาให้ยืนยันก่อน

=== เกณฑ์ผ่านที่ต้องทดสอบเองก่อนรายงาน ===
  - แถบส่วนแสดง 6 ส่วนตามลำดับใหม่ ทั้ง th และ en
  - ไม่เหลือคำว่า "LO ชุดแรก"/"LO ชุดสอง"/"ด้านที่ 6 ด้านอื่น ๆ:"
  - เลข "ส่วนที่ N" ในหัวข้อกลุ่มทุกอัน ตรงกับตำแหน่งจริงบนแถบส่วน
  - ข้อ 6 พรีเมี่ยม ไม่ตอบ → กดส่งไม่ผ่าน
  - เลือก "ทบทวนดูก่อน" → เด้งช่องเหตุผล เว้นว่างแล้วกดส่งไม่ผ่าน
  - เลือก "ใช่"/"ไม่ใช่" แล้วสลับกลับ → เหตุผลเดิมไม่ค้างไปกับ payload
  - ส่วนรายงานแสดง 4 การ์ด และ submit ค่า 5 แล้ว server ปฏิเสธ
  - grep -rn "adv5ScoreSchema\|rating5" web/src ไม่เจอผลลัพธ์
  - ร่างเก่าที่บันทึกด้วยลำดับเดิม เปิดแล้วไม่เด้งผิดส่วนหรือพัง
  - กดข้ามส่วนได้อิสระ ไม่มี validation บล็อก
  - npm run build และ npm run lint ผ่าน

=== รายงานกลับ ===
จำนวน submission จริงที่นับได้ + รายการ default ที่ใช้ + ผล build/lint
+ ถ้า Agent 2 สร้าง lib/coop-center-copy.ts แล้ว ให้ต่อสาย advisor-copy.ts
  ให้ import จากโมดูลนั้นแทนการเก็บข้อความซ้ำ
```

---

# Agent 4 — คู่มือการใช้งาน (ขนานได้ตั้งแต่ต้น)

```
โปรเจกต์: ระบบแบบประเมินผลลัพธ์การเรียนรู้รายวิชาสหกิจศึกษา
อ่าน goal.md ส่วน G8 ให้จบก่อนเริ่ม

คุณเขียนเอกสาร ไม่แก้โค้ด
ไฟล์ที่คุณเป็นเจ้าของ: docs/manual/* เท่านั้น
ห้ามแก้ไฟล์ใน web/ ทุกไฟล์ ถ้าเจอบั๊กระหว่างทาง ให้จดไว้รายงาน ไม่ต้องแก้เอง

=== เป้าหมาย ===

คู่มือที่ส่งให้สถานประกอบการอ่านแล้วกรอกฟอร์มได้เองจนจบ โดยไม่ต้องโทรถาม

กลุ่มผู้อ่าน: พี่เลี้ยง/หัวหน้างานในสถานประกอบการ ส่วนใหญ่ "ไม่ถนัดเทคโนโลยี"
สมมติว่าผู้อ่านไม่รู้ว่าปุ่มถัดไปอยู่ตรงไหน ทำไมกดแล้วไม่ไป กรอกค้างแล้วกลับมาทำต่อยังไง

หลักการเขียน:
  - เขียนเป็นคำสั่งทีละขั้น ("กดปุ่ม ถัดไป ที่มุมขวาล่าง") ไม่ใช่คำบรรยายระบบ
  - ห้ามใช้ศัพท์เทคนิค — ไม่มีคำว่า browser, cache, validation, draft, localStorage
    ในตัวคู่มือ ให้อธิบายด้วยคำชาวบ้านแทน
  - ทุกขั้นตอนที่ต้องกด ต้องมีภาพหน้าจอพร้อมกรอบ/ลูกศรชี้จุด

=== ลำดับการทำงาน (สำคัญ) ===

เฟส A — เขียนข้อความก่อน (เริ่มได้เลยตอนนี้)
เฟส B — ถ่ายภาพหน้าจอ "หลัง Agent 2 และ 3 เสร็จเท่านั้น"
        ถ้าถ่ายก่อน ภาพจะใช้ไม่ได้ทั้งหมด เพราะโครงสร้างส่วนเปลี่ยนทั้งสองฟอร์ม
        และสเกลรายงานเปลี่ยนจาก 5 เป็น 4 การ์ด

=== โครงเรื่อง 10 หัวข้อ ===
ดูตารางเต็มใน G8 ของ goal.md — สรุป:
  1 แบบประเมินนี้คืออะไร ใครกรอก     2 เตรียมข้อมูลก่อนเริ่ม (เช็กลิสต์)
  3 เข้าระบบยังไง                      4 ทำความรู้จักหน้าจอ
  5 กรอกทีละส่วน (6 ส่วน)             6 กรอกค้างแล้วกลับมาทำต่อ
  7 จะแก้คำตอบต้องทำยังไง             8 กดส่ง + รหัสอ้างอิง
  9 ปัญหาที่พบบ่อย                    10 ติดต่อใคร

=== 4 เรื่องที่ "ต้อง" เตือน ไม่งั้นหน่วยงานโทรเข้าแน่ ===

ทั้ง 4 ข้อนี้ตรวจจากพฤติกรรมจริงของโค้ดแล้ว ไม่ใช่การเดา อ่านรายละเอียดใน G8

1. ร่างที่กรอกค้างผูกกับเครื่องเดิม + เบราว์เซอร์เดิมเท่านั้น
   เปลี่ยนเครื่อง / เปิดโหมดไม่ระบุตัวตน / ล้างประวัติ → ร่างหายหมด
   ต้องเขียนว่า "ให้กรอกจนจบด้วยเครื่องเดิมและโปรแกรมเดิมที่เริ่มไว้"

2. ส่งแล้วแก้เองไม่ได้
   ต้องเตือนก่อนถึงปุ่มส่ง และบอกทางออก: ติดต่อเจ้าหน้าที่พร้อมแจ้งรหัสอ้างอิง

3. ปุ่ม "แก้ไข" บนแถบเครื่องมือไม่ใช่ปุ่มแก้คำตอบ
   มันคือหน้าแก้ชุดคำถามสำหรับเจ้าหน้าที่ และไม่มีระบบล็อกอินป้องกัน
   ต้องเขียนเตือนว่า "สำหรับเจ้าหน้าที่ ไม่ต้องกด"

4. กดข้ามส่วนได้ และจะไม่มี error จนกว่าจะกดส่ง
   เป็นพฤติกรรมที่ตั้งใจออกแบบ ไม่ใช่ระบบพัง
   ต้องอธิบายว่าเครื่องหมายถูกบนแถบส่วน = ตอบครบแล้ว ใช้เช็กก่อนกดส่ง
   และถ้ากดส่งแล้วไม่ครบ ให้กดรายการในกล่องแดงด้านบน ระบบจะพาไปช่องที่ต้องแก้

=== ตารางปัญหาที่พบบ่อย ===
คัดลอกตารางจาก G8 ใน goal.md มาใช้ได้เลย มี 7 แถวพร้อมสาเหตุและวิธีแก้

=== ไฟล์ปลายทาง ===
docs/manual/คู่มือการใช้งาน-สถานประกอบการ.md
ภาพใน docs/manual/images/
แล้ว export เป็น PDF สำหรับส่งให้หน่วยงาน

=== ข้อจำกัดที่ยังไม่ได้คำตอบ (Q7–Q10 ใน goal.md) ===
  - ทำ 1 เล่มหรือ 2 เล่ม (สถานประกอบการ / อาจารย์นิเทศ) — default: ทำเล่มสถานประกอบการก่อน
  - ต้องมีฉบับภาษาอังกฤษไหม — default: ทำไทยก่อน วางโครงให้แปลง่าย
  - หลังส่งแล้วแก้ได้ไหม — default: เขียนว่าติดต่อเจ้าหน้าที่
ใช้ default แล้วเขียน TODO กำกับไว้ อย่าเดาเอง

=== ข้อมูลติดต่อ ===
ยังไม่มีข้อมูลจริงของเจ้าหน้าที่ศูนย์สหกิจฯ
ให้ใส่ placeholder ที่เห็นชัดว่ายังไม่ได้เติม เช่น [ชื่อเจ้าหน้าที่ — รอข้อมูล]
ห้ามแต่งชื่อ เบอร์โทร หรืออีเมลขึ้นมาเอง

=== เกณฑ์ผ่าน ===
  - คนที่ไม่เคยเห็นระบบ อ่านคู่มืออย่างเดียวแล้วกรอกจนกดส่งได้ โดยไม่ถามใคร
  - ทุกขั้นตอนที่ต้องกด มีภาพประกอบชี้จุด
  - มีคำเตือนครบทั้ง 4 เรื่อง
  - ไม่มีศัพท์เทคนิคหลงเหลือ
  - ไม่มีข้อมูลติดต่อที่แต่งขึ้นเอง

=== รายงานกลับ ===
รายการ TODO/placeholder ที่ยังต้องเติม + บั๊กหรือจุดสับสนของ UI ที่เจอระหว่างเขียน
(ข้อหลังมีค่ามาก — คนเขียนคู่มือมักเจอปัญหา UX ที่คนเขียนโค้ดมองไม่เห็น)
```

---

# Agent 5 — ฟอร์มนักศึกษา (ฟอร์มใหม่ทั้งฟอร์ม · รันหลัง Agent 2)

```
โปรเจกต์: ระบบแบบประเมินผลลัพธ์การเรียนรู้รายวิชาสหกิจศึกษา (Next.js + Postgres)
อ่าน goal.md ส่วน G10 ให้ครบทุกตัวอักษร แล้วอ่าน CONTEXT.md และ web/CLAUDE.md
ต้นฉบับกระดาษ: "Coop Evaluation Form _ ระบบแบบประเมิน ศูนย์สหกิจศึกษา ม.วลัยลักษณ์.pdf"
(ถ้าเปิดไม่ได้ ให้ใช้รายการคำถามเต็มที่ถอดไว้แล้วใน G10 ได้เลย ถอดครบทั้ง 33 ข้อ)

นี่เป็นงานใหญ่ที่สุดใน phase นี้ — ฟอร์มที่ 3 ของระบบ ไม่ใช่การแก้ของเดิม
ระบบเดิมมี 2 ฟอร์ม (สถานประกอบการ, อาจารย์นิเทศ) คุณกำลังเพิ่มตัวที่ 3

ไฟล์ที่คุณเป็นเจ้าของ (เป็นไฟล์ใหม่ทั้งหมด):
  web/src/components/student/*
  web/src/lib/student-schema.ts
  web/src/app/[school]/[program]/student/*
ไฟล์เดิมที่ได้รับอนุญาตให้แตะ "เฉพาะการเพิ่ม role student" เท่านั้น:
  web/src/lib/routes.ts                              (isFormRole รับ "student")
  web/src/app/[school]/[program]/[role]/[lang]/page.tsx  (เพิ่มสาขา role === "student")
  web/src/components/ui/form-picker-dialog.tsx       (เพิ่มตัวเลือกที่ 3)
  CONTEXT.md                                         (เพิ่ม glossary)
ห้ามแตะไฟล์ของ Agent 2 และ 3 (components/evaluation/*, components/advisor/*,
lib/evaluation-schema.ts, lib/advisor-schema.ts) ถ้าต้องแก้ ให้หยุดแล้วรายงาน

=== ตัวตนของฟอร์ม ===
ชื่อเต็ม : แบบสอบถามนักศึกษาหลังกลับจากการปฏิบัติงานสหกิจศึกษา
ผู้กรอก  : นักศึกษา (ประเมินตนเอง)
role     : student
URL      : /{schoolSlug}/{programKey}/student/{locale}

คำชี้แจงที่ต้องแสดงเด่นที่หัวฟอร์ม (ห้ามซ่อน):
  "กรุณาให้ข้อมูลตามความเป็นจริง ข้อมูลนี้ไม่มีผลต่อการประเมินผลปฏิบัติงาน
   และใช้เพื่อพัฒนาการจัดสหกิจศึกษาเท่านั้น"
เหตุผล: ถ้านักศึกษาคิดว่าคำตอบมีผลต่อเกรด จะได้ข้อมูลที่ไม่จริง

=== หัวใจของงาน: 2 เรื่องที่ต่างจากต้นฉบับ ===

เรื่องที่ 1 — ส่วน "การประเมินตนเอง" ต้องเป็น LO ของหลักสูตร ไม่ใช่คำถามตายตัว

ต้นฉบับมีคำถามตายตัว 22 ข้อ เหมือนกันทุกหลักสูตร
ต้องเปลี่ยนเป็น: ดึง LO ของหลักสูตรนั้นจาก DB แล้วแบ่งเป็น 2 ท่อน
(ความรู้และทักษะ / จริยธรรมและบุคลิก) เหมือนอีก 2 ฟอร์ม

ใช้ตรรกะเดิมทั้งหมด อย่าเขียนใหม่:
  PRIMARY_DOMAINS, dedupeByLoCode, questionText, optionLabel, sectionTitle
  ดูวิธีใช้ที่ app/[school]/[program]/[role]/[lang]/page.tsx และ
  components/evaluation/steps.tsx -> CompetencyStep

เรื่องที่ 2 — เลขข้อต้องรันใหม่ตอน render ห้าม hard-code

เพราะจำนวน LO ต่างกันทุกหลักสูตร (บางหลักสูตร 8 ข้อ บางหลักสูตร 15 ข้อ)
เลขข้อของส่วนที่ 5 และ 6 จึงเลื่อนตาม

  N = จำนวนคำถาม LO ทั้งหมดของหลักสูตรนั้น (ชุดแรก + ชุดสอง)
  เลขข้อของส่วนที่ 5 เริ่มที่ N + 1 แล้วเดินต่อเนื่องข้ามส่วนไปจนจบ

ห้ามเขียนเลข 23-33 ลงในโค้ดเด็ดขาด ในเอกสาร goal.md เขียนเป็น N+k ด้วยเหตุผลนี้
ต้องมีตัวนับเลขข้อตัวเดียวที่เดินต่อเนื่องทั้งฟอร์ม ไม่ใช่ให้แต่ละส่วนเริ่มนับ 1 ใหม่

=== สเกลคะแนน: 4 ระดับทั้งฟอร์ม (ต่างจากต้นฉบับที่ใช้ 1-5) ===

  LO (ส่วนที่ 3-4)                    : 4 ระดับตาม rubric จาก DB (assessment_options)
  ศูนย์สหกิจฯ + สถานประกอบการ (ส่วนที่ 6) : 4 ระดับ — 4 ดีเยี่ยม / 3 ดี / 2 พอใช้ / 1 ควรปรับปรุง

ต้นฉบับ PDF ใช้ 1-5 (น้อยสุด...มากสุด) ทุกข้อ — "อย่าทำตามต้นฉบับ"
ผู้ใช้ยืนยันแล้วว่าให้เป็น 4 ระดับ เพื่อให้เทียบคะแนนกับอีก 2 ฟอร์มได้ตรง ๆ
และตรงกับกติกา "4 ระดับทั้งระบบ ไม่มีข้อยกเว้น" ใน CONTEXT.md (ที่ Agent 1 อัปเดตแล้ว)

=== โครงสร้าง 6 ส่วน ===

  ส่วนที่ 1  ข้อมูลทั่วไป              (รหัส นศ. -> ที่ตั้งหน่วยงาน)
  ส่วนที่ 2  ค่าใช้จ่ายและที่พัก      (ค่าตอบแทน/สวัสดิการ -> แนะนำที่พัก)
  ส่วนที่ 3  ความรู้และทักษะ          <- LO ชุดแรก จาก DB
  ส่วนที่ 4  จริยธรรมและบุคลิก        <- LO ชุดสอง จาก DB
  ส่วนที่ 5  จุดเด่น / สิ่งที่ต้องพัฒนา
  ส่วนที่ 6  ศูนย์สหกิจฯ + สถานประกอบการ + ข้อคิดเห็น

รายการคำถามเต็มทุกข้อพร้อมชนิดฟิลด์และสถานะบังคับ อยู่ในตารางของ G10 ใน goal.md
ทำตามตารางนั้น ไม่ต้องตีความ PDF ใหม่

=== รายละเอียดที่พลาดง่าย ===

1. สำนักวิชา และ หลักสูตร ต้องเติมอัตโนมัติจาก URL แล้วล็อกเป็น read-only
   ไม่ใช่ให้นักศึกษาเลือกเอง (ต้นฉบับให้เลือก) — กันกรอกผิดหลักสูตรแล้วได้ชุด LO ผิด
   ดูวิธีทำที่ GeneralStep ในฟอร์มสถานประกอบการ

2. ต้นฉบับ PDF มีเลขข้อ 4 ซ้ำกัน 2 ครั้ง ("4. หลักสูตร" และ "4. ภาคการศึกษา")
   เป็นบั๊กของต้นฉบับ ตารางใน goal.md แก้ให้แล้ว ใช้ตามตาราง

3. ข้อค่าที่พัก/ค่าอาหาร/ค่าเดินทาง ต้นฉบับเขียน "รวมทั้ง 8 เดือน"
   ผู้ใช้ยืนยันให้ "ตัดเลขเดือนออก ใช้คำว่า ตลอดการปฏิบัติงาน" แทน

4. ข้อ "ค่าตอบแทนหรือสวัสดิการ" เลือก "ไม่มี" -> ต้องซ่อนข้อย่อย 8.1-8.3
   และล้างค่าไม่ให้ค้างใน payload
   ใช้แพตเทิร์น disclosure เดิมที่ advisor-schema.ts -> adv_future_placement
   + advisor-steps-extra.tsx:243 อย่าคิดใหม่

5. คำถาม 2 ข้อเรื่องศูนย์สหกิจฯ ในส่วนที่ 6 "ต้อง import จาก lib/coop-center-copy.ts"
   ที่ Agent 2 สร้างไว้ ห้ามเก็บถ้อยคำชุดที่สาม
   ถ้อยคำในต้นฉบับนักศึกษาต่างจากอีก 2 ฟอร์มเล็กน้อย -> ยึดฉบับในโมดูลกลาง
   (Q14 ใน goal.md ใช้ default นี้ไปก่อน เขียน TODO กำกับ)

6. ฟอร์มเป็น uncontrolled + มี draft restore
   ทุก state ต้อง sync จาก DOM ผ่าน formVersion ดูแพตเทิร์นใน CompetencyStep
   ไม่งั้นกู้ร่างแล้วค่าไม่ขึ้น

=== สิ่งที่ต้องนำกลับมาใช้ซ้ำ (อย่าเขียนใหม่) ===
  RatingCard (+ prop disabledValues ที่ Agent 1 เพิ่มไว้)
  ErrorSummary, StepProgressBar, Stepper, AutosaveStatus
  components/evaluation/fields.tsx (Field, SelectField, TextAreaField, Required)
  ChoiceGroup
  step constants pattern ที่ Agent 1 วางไว้ (ใช้ชื่อรูปแบบเดียวกัน STUDENT_STEP)
  แพตเทิร์น draft autosave ใน EvaluationWizard (loadDraft / persistDraft / restoreForm)

ถ้าเจอว่าต้อง copy โค้ดเกิน 20 บรรทัดจากฟอร์มเดิม ให้หยุดแล้วรายงานมา
อาจแปลว่าควรยกส่วนนั้นขึ้นเป็นโมดูลกลางแทน แต่นั่นต้องแตะไฟล์ของ agent อื่น

=== ฐานข้อมูล ===
ตาราง evaluation_submissions เดิมออกแบบมาเพื่อคะแนน LO + รายงาน
ไม่มีที่เก็บข้อมูลค่าใช้จ่าย/ที่พัก (Q16 ใน goal.md ยังไม่ได้คำตอบ)
ใช้ default นี้ไปก่อน: ใช้ตารางเดิม + คอลัมน์ evaluator_role = 'student'
แล้วเก็บรายละเอียดทั้งหมดลง payload_json พร้อมเขียน TODO กำกับ
ห้ามรัน migration กับฐานข้อมูลจริงเอง เขียนสคริปต์แล้วรายงานมาให้ยืนยันก่อน

=== ภาษาอังกฤษ: ต้องมี (Q17 ปิดแล้ว ผู้ใช้ยืนยัน 2026-09-11) ===

ฟอร์มนี้ "ต้องรองรับ 2 ภาษาเต็มรูปแบบ" เหมือนอีก 2 ฟอร์ม
ไม่มีทางลัด ห้าม redirect /en -> /th

โครงสร้าง copy ต้องเป็น Record<Locale, ...> ตั้งแต่แรก และเติมครบทั้ง th และ en

ข้อความที่ "มีคำแปลอังกฤษอยู่แล้ว ให้ดึงมาใช้ ห้ามแปลใหม่":
  คำถามศูนย์สหกิจฯ 2 ข้อ          -> lib/coop-center-copy.ts (Agent 1 สร้างไว้)
  ชื่อส่วน ความรู้และทักษะ / จริยธรรมและบุคลิก -> WIZARD_COPY[en].steps
  คำถาม LO ทุกข้อ                  -> text_en / label_en / description_en จาก DB
  ป้ายสเกล 4 ระดับ                 -> ADVISOR_COPY[en].rating4
  ฟิลด์ร่วม (รหัส นศ., ชื่อ-สกุล, สำนักวิชา, หลักสูตร, ภาคการศึกษา) -> WIZARD_COPY[en]

ที่ต้องแปลใหม่จริง ๆ มีแค่ 2 กลุ่ม:
  ส่วนที่ 2 ค่าใช้จ่ายและที่พัก (รวมป้ายสวัสดิการ 7 รายการ)
  กลุ่มการสนับสนุนจากสถานประกอบการ ในส่วนที่ 6

สำหรับ 2 กลุ่มนี้: ร่างคำแปลมาให้ดูได้ แต่ "ต้องรวบรวมเป็นรายการแยกในรายงาน"
เพื่อให้ศูนย์สหกิจฯ ตรวจก่อนใช้จริง อย่าถือว่าคำแปลของคุณเป็นฉบับสุดท้าย

ตรวจก่อนรายงาน: เปิด /en แล้วต้องไม่มีอักษรไทยโผล่แม้แต่ตัวเดียว
และเปิด /th แล้วต้องไม่มีอักษรละตินในข้อความ UI

=== เกณฑ์ผ่านที่ต้องทดสอบเองก่อนรายงาน ===
  - เปิด /{school}/{program}/student/th ได้ ไม่ 404
  - ส่วนที่ 3-4 แสดง LO ของหลักสูตรนั้นจริง ไม่ใช่คำถามตายตัว 22 ข้อ
  - เปิดคนละหลักสูตร (ที่มีจำนวน LO ต่างกัน) -> ได้ชุด LO คนละชุด
    "และเลขข้อของส่วนที่ 5-6 เลื่อนตามถูกต้อง" -- ทดสอบอย่างน้อย 2 หลักสูตร
  - ทุกข้อคะแนนเป็น 4 ระดับ ไม่มี 5 ระดับหลงเหลือ
  - เลือก "ไม่มี" ในข้อค่าตอบแทน -> ซ่อนข้อย่อย และค่าไม่ค้างใน payload
  - คำชี้แจง "ไม่มีผลต่อการประเมินผลปฏิบัติงาน" แสดงเด่นที่หัวฟอร์ม
  - กดข้ามส่วนได้อิสระทุกส่วน ไม่มี validation บล็อก (กติกาข้อ 1)
  - กดส่งโดยไม่กรอก -> ขึ้นกล่องสรุปข้อผิดพลาด และกดแล้วเด้งไปช่องที่ถูกต้อง
  - บันทึกร่างแล้วรีเฟรช -> ค่ากลับมาครบทุกส่วน รวมถึงข้อย่อยที่ซ่อนอยู่
  - หน้าเลือกฟอร์มแสดง 3 ตัวเลือก ไม่ใช่ 2
  - npm run build และ npm run lint ผ่าน

=== รายงานกลับ ===
1. รายการ default ที่ใช้เพราะยังไม่ได้คำตอบ (Q14-Q16) พร้อม path ของ TODO ที่ฝังไว้
2. "รายการคำแปลอังกฤษที่คุณร่างเอง" (ส่วนค่าใช้จ่าย/ที่พัก + การสนับสนุนจากสถานประกอบการ)
   แยกเป็นตาราง ไทย | อังกฤษ ให้ศูนย์สหกิจฯ ตรวจ
3. สคริปต์ migration ที่เขียนไว้ (ถ้ามี) — ยังไม่รัน
4. ผล npm run build และ npm run lint
5. จุดที่ต้อง copy โค้ดจากฟอร์มเดิมเยอะ และคิดว่าควรยกเป็นโมดูลกลางใน phase หน้า
```

---

## หมายเหตุก่อนสั่งรัน

1. **Agent 1 ต้องเสร็จก่อน** — Agent 2, 3, 5 พึ่ง step constants และ prop ใหม่ของ `RatingCard` ถ้ารันพร้อมกันจะชนกันและต้องทำใหม่
2. **`lib/coop-center-copy.ts` เป็นจุดประสานของ 3 agent** — Agent 2 สร้าง แล้ว Agent 3 กับ Agent 5 ต่อสาย คำถามประเมินศูนย์สหกิจฯ 2 ข้อนี้ไปโผล่ทั้ง 3 ฟอร์ม ถ้าปล่อยให้ต่างคนต่างเก็บถ้อยคำ จะ drift แน่นอน
3. **Agent 5 ต้องรอ Agent 2** ด้วยเหตุผลข้อ 2 — ถ้าอยากรันขนานจริง ๆ ให้ Agent 1 เป็นคนสร้าง `coop-center-copy.ts` แทน (ย้ายงานนี้จาก Agent 2 ไป Agent 1) แล้ว Agent 2/3/5 จะขนานได้หมด **นี่คือทางที่ผมแนะนำถ้าเร่งเวลา**
4. **คำถามค้าง Q1, Q3, Q5, Q7–Q17 ยังไม่มีคำตอบ** — ทุก agent ได้รับคำสั่งให้ใช้ default แล้วเขียน TODO ไม่ใช่เดาเอง ตรวจรายการ default ในรายงานของแต่ละตัวก่อน merge
5. **Q11 (ซ่อนปุ่มแก้ไขจากคนนอก) ยังไม่ได้มอบหมายให้ใคร** — เป็นเรื่องความปลอดภัยที่ควรตัดสินใจก่อนส่งลิงก์ให้สถานประกอบการจริง ถ้าตกลงจะทำ ให้เพิ่มเข้างานของ Agent 1
6. **Q17 (ภาษาอังกฤษของฟอร์มนักศึกษา) ควรตอบก่อนสั่ง Agent 5** — ถ้าตอบว่าต้องมี `en` ต้องหาคนแปล ~40 ข้อความ ซึ่งไม่ใช่งานที่ agent ควรเดาเอง
