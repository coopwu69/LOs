# goal.md — แผนพัฒนาระบบ Phase ถัดไป

> **ที่มา:** feedback จากผู้ตรวจ รอบวันที่ 11 กันยายน 2569 (2026-09-11)
> **ขอบเขต phase นี้:** แก้ **ฝั่งแบบประเมินจากสถานประกอบการ** (`role=company`) เป็นหลัก + งานที่กระทบทั้งระบบ (ชื่อฟอร์ม, rubric, สเกลรายงาน) + **คู่มือการใช้งานสำหรับส่งให้หน่วยงานตรวจ**
> **ฝั่งอาจารย์นิเทศ (`role=advisor`) แก้ด้วย:** ชื่อฟอร์ม ([G1](#g1--ชื่อแบบประเมินเต็ม-ๆ-ทั้งสองฝั่ง)), rubric ([G3](#g3--rubric-ของ-los-ตัดป้ายระดับออก-เหลือเลข--คำอธิบาย)), สเกลรายงาน 5→4 ([G4](#g4--รายงานโครงงาน-ลดจาก-5-ระดับเหลือ-4-ระดับ)) และ **จัดลำดับส่วนใหม่ทั้งฟอร์ม** ([G9](#g9--จัดโครงสร้างฝั่งอาจารย์นิเทศใหม่))
> **ฟอร์มใหม่ทั้งฟอร์ม:** แบบสอบถามนักศึกษาหลังกลับจากการปฏิบัติงาน (`role=student`) ([G10](#g10--ฟอร์มใหม่-แบบสอบถามนักศึกษาหลังกลับจากการปฏิบัติงานสหกิจศึกษา)) — **ระบบจะมี 3 ฟอร์ม** ไม่ใช่ 2 ฟอร์มอีกต่อไป
> **สถานะ (อัปเดต 2026-09-12):** **G1, G2, G3, G4 (โค้ด), G5, G6, G9, G10 เสร็จและ verified สดแล้ว** (ดูหมายเหตุ ✅ ในแต่ละหัวข้อ) — push ขึ้น `main` ครบ ค้างอยู่ 3 เรื่อง: (1) รัน migration 019 จริงกับ DB — ต้องให้ผู้ใช้รันเอง (auto-mode ปฏิเสธการเขียนฐานข้อมูล production โดยตรง), (2) **G11** รอผู้ใช้ตรวจสไตล์ตัวอย่าง rubric ก่อนร่างที่เหลือ, (3) **G8** รอข้อมูลติดต่อเจ้าหน้าที่จริงก่อนถ่ายภาพหน้าจอ+ปิดคู่มือ. **G7 ยังไม่ทำตามแผนเดิม.** ผู้ใช้แจ้งว่าจะทยอยเล่า feedback เพิ่ม ให้ถือว่าเอกสารนี้เป็นฉบับมีชีวิต (living document)

---

## สารบัญ

| รหัส | เรื่อง | ความเสี่ยง | ขึ้นกับ |
|---|---|---|---|
| [G1](#g1--ชื่อแบบประเมินเต็ม-ๆ-ทั้งสองฝั่ง) | ชื่อแบบประเมินเต็ม ๆ ทั้งสองฝั่ง | ต่ำ | — |
| [G2](#g2--ส่วนที่-2-ใหม่-ทักษะที่สถานประกอบการคาดหวังจากนักศึกษา) | ส่วนที่ 2 ใหม่: ทักษะที่สถานประกอบการคาดหวัง (16 ข้อ) | **สูง** | G6 |
| [G3](#g3--rubric-ของ-los-ตัดป้ายระดับออก-เหลือเลข--คำอธิบาย) | rubric ของ LOs — ตัดป้ายระดับออก | ต่ำ | — |
| [G4](#g4--รายงานโครงงาน-ลดจาก-5-ระดับเหลือ-4-ระดับ) | รายงาน/โครงงาน ลดเหลือ 4 ระดับ | **สูง** (กระทบข้อมูลเดิม) | — |
| [G5](#g5--ย้ายและแปลงส่วนประเมินกระบวนการศูนย์สหกิจศึกษาฯ) | ย้าย+แปลงส่วนประเมินกระบวนการศูนย์สหกิจฯ | กลาง | G6 |
| [G6](#g6--โครงสร้างใหม่-6-ส่วน-ฝั่งสถานประกอบการ) | โครงสร้างใหม่ 6 ส่วน ฝั่งสถานประกอบการ | กลาง | G2, G5 |
| [G7](#g7--คะแนนถ่วงน้ำหนัก-los-35--รายงาน-25--อาจารย์นิเทศ-40-บันทึกไว้-ยังไม่ทำ) | คะแนนถ่วงน้ำหนัก 35/25/40 | — | **เลื่อนไป phase ถัดไป** |
| [G8](#g8--คู่มือการใช้งานระบบ-สำหรับส่งให้หน่วยงานตรวจ) | คู่มือการใช้งานระบบ (ส่งให้หน่วยงานตรวจ) | กลาง | G1–G6, G9 (ต้องถ่ายภาพหลัง UI นิ่ง) |
| [G9](#g9--จัดโครงสร้างฝั่งอาจารย์นิเทศใหม่) | จัดโครงสร้างฝั่งอาจารย์นิเทศใหม่ | **สูง** | G4 |
| [G10](#g10--ฟอร์มใหม่-แบบสอบถามนักศึกษาหลังกลับจากการปฏิบัติงานสหกิจศึกษา) | **ฟอร์มใหม่: แบบสอบถามนักศึกษา** | **สูงสุด** | G4, G5 (ใช้ `coop-center-copy`) |
| [G11](#g11--ย่อคำอธิบาย-rubric-ที่ยาวเกินจำเป็น) | ย่อคำอธิบาย rubric (232 ข้อ, ตัดได้ 27%) | กลาง (แตะ DB) | — (Claude ทำเอง) |

---

## G1 — ชื่อแบบประเมินเต็ม ๆ ทั้งสองฝั่ง

> ✅ **เสร็จแล้ว** (commit aa37a13) — verified สดใน browser: หัวฟอร์มทั้งสองฝั่ง, form-picker dialog, แท็บเบราว์เซอร์ (role-aware generateMetadata), หน้าขอบคุณ. `grep -r "แบบประเมินหน่วยงาน" web/src` เหลือแต่ comment อธิบายการเปลี่ยนชื่อ ไม่ใช่ข้อความที่แสดงจริง. ไฟล์ Word/หน้าพิมพ์ใช้ `TEMPLATE_DOC_TITLE` (ชื่อกลาง ไม่ใช่ FORM_NAMES) เพราะเป็นเอกสาร LO template ที่ใช้ร่วมกันทั้งสองฟอร์ม ไม่ใช่ของฟอร์มใดฟอร์มหนึ่ง — ดูเหตุผลใน commit. อังกฤษยังเป็นร่างรอศูนย์สหกิจฯ ยืนยัน (Q1).

### สิ่งที่ต้องได้

ชื่อเต็มที่ถูกต้อง (ใช้แทนคำเดิม "แบบประเมินหน่วยงาน" / "แบบประเมินอาจารย์นิเทศ" ทุกจุด):

| ฝั่ง | ชื่อเต็มภาษาไทย |
|---|---|
| `company` | **แบบประเมินผลการปฏิบัติสหกิจศึกษาจาก*สถานประกอบการ*** |
| `advisor` | **แบบประเมินนักศึกษาสหกิจศึกษาจาก*อาจารย์นิเทศ*** |

**หลักการ:** ต้องเน้นคำว่า **สถานประกอบการ** และ **อาจารย์นิเทศ** ให้เห็นชัดในประโยค — ผู้กรอกต้องรู้ทันทีว่าตัวเองกำลังกรอกฟอร์มของใคร ห้ามย่อเหลือ "แบบประเมินหน่วยงาน" อีก

### จุดที่ต้องไปแก้

| ไฟล์ | บรรทัด/จุด | ปัจจุบัน |
|---|---|---|
| `web/src/app/[school]/[program]/[role]/[lang]/page.tsx` | ~114 | แสดง "แบบประเมินอาจารย์นิเทศ" เฉพาะตอน `role !== "company"` — ฝั่ง company ไม่มีชื่อฟอร์มเลย |
| `web/src/components/ui/form-picker-dialog.tsx` | `COPY.th.companyName` / `advisorName` | "แบบประเมินหน่วยงาน" / "แบบประเมินอาจารย์นิเทศ" |
| `web/src/components/ui/programs-list.tsx` | ~36 `pickerHint` | "…ระหว่างแบบประเมินหน่วยงานและแบบประเมินอาจารย์นิเทศ" |
| `web/src/app/programs/[programId]/print/page.tsx` | ~28 | fallback "แบบประเมิน LOs รายวิชาสหกิจศึกษา" |
| `web/src/app/programs/[programId]/export/docx/route.ts` | ~231, 329–331, 376 | หัวเรื่องและ metadata ของไฟล์ Word |
| `web/src/app/layout.tsx` | ~19 | `metadata.title` (ชื่อแท็บเบราว์เซอร์) |
| `web/src/components/evaluation/CompletionScreen.tsx` + `components/evaluation/copy.ts` (`successTitle`) | | "ส่งแบบประเมินเรียบร้อยแล้ว" → ระบุชื่อฟอร์ม |
| `web/src/components/advisor/AdvisorCompletionScreen.tsx` | ~40 | "ส่งแบบประเมินอาจารย์นิเทศเรียบร้อยแล้ว" |
| `web/src/components/FormShell.tsx` | ~100 | ชื่อระบบใน shell |
| `CONTEXT.md` | glossary | ต้องอัปเดตคำศัพท์มาตรฐาน 2 รายการ |

### เกณฑ์ผ่าน

- [ ] เปิด `/{school}/{program}/company/th` แล้วเห็นชื่อเต็มฝั่งสถานประกอบการที่หัวฟอร์ม
- [ ] เปิด `/{school}/{program}/advisor/th` แล้วเห็นชื่อเต็มฝั่งอาจารย์นิเทศที่หัวฟอร์ม
- [ ] หน้าเลือกฟอร์ม (dialog) + รายการหลักสูตร ใช้ชื่อเต็ม
- [ ] ไฟล์ Word / หน้าพิมพ์ ขึ้นชื่อเต็ม
- [ ] ชื่อแท็บเบราว์เซอร์และหน้าขอบคุณหลังส่ง ขึ้นชื่อเต็ม
- [ ] `grep -r "แบบประเมินหน่วยงาน" web/src` ไม่เจอผลลัพธ์

### ⚠️ ค้างรออนุมัติ — คำแปลภาษาอังกฤษ

`CONTEXT.md` ล็อกกติกา "แยกภาษาเด็ดขาด" ไว้ โหมด `en` ห้ามมีไทยปน จึงต้องมีคำแปลคู่กัน ผมเสนอ:

- `company` → **Cooperative Education Performance Evaluation by the Workplace**
- `advisor` → **Cooperative Education Student Evaluation by the Faculty Advisor**

ของเดิมในระบบใช้ `Workplace evaluation` / `Advisor evaluation` **ต้องให้ผู้ใช้ยืนยันก่อนแก้** (ดู [คำถามค้าง Q1](#q1--คำแปลภาษาอังกฤษของชื่อฟอร์ม))

---

## G2 — ส่วนที่ 2 ใหม่: ทักษะที่สถานประกอบการคาดหวังจากนักศึกษา

> ✅ **เสร็จแล้ว** (commit f207169) — verified สดใน browser: ติ๊ก/ไม่ติ๊กไม่บล็อก navigation, สเกลเด้งออกมาทันที, ระดับ 1 คลิกไม่ติด (ยืนยันด้วย DOM: disabled=true), ติ๊กออก → RatingCard unmount ทั้งกลุ่ม (ไม่มีทางค้างค่า), submit ว่างเปล่า → error "skills" ขึ้นถูกต้อง. ยังไม่ได้ทดสอบแยกกรณี "กรอกครบทุกส่วนยกเว้นทักษะ" เพื่อยืนยันว่าเด้งไปส่วนที่ 2 เป๊ะ ๆ (โค้ดคำนวณถูกต้องตาม fieldStep() แต่ไม่ได้ isolate-test เคสนี้โดยเฉพาะ).
**งานชิ้นใหญ่ที่สุดของ phase นี้** — เป็น question type ใหม่ที่ระบบยังไม่มี

### หัวข้อส่วน

```
ส่วนที่ 2   เลือกทักษะที่สถานประกอบการคาดหวังจากนักศึกษา
Part 2:     Select the skills expected by the employer.
```

### พฤติกรรมที่ต้องการ

1. แสดงทักษะ **16 ข้อ** เป็น checkbox (multi-select) — ควรจัดเป็น 2 คอลัมน์บนจอกว้าง ตามต้นฉบับกระดาษ
2. **บังคับเลือกอย่างน้อย 1 ข้อ ไม่จำกัดสูงสุด**
3. **เมื่อติ๊กข้อไหน → เด้งสเกลระดับความจำเป็นของข้อนั้นออกมา** (progressive disclosure)
4. สเกลระดับความจำเป็น **แสดงครบ 5 ระดับ แต่ระดับ 1 ต้อง `disabled` กดไม่ได้**
   - เหตุผล: การที่ผู้ประเมินติ๊กข้อนี้มา แปลว่าทักษะนี้เกี่ยวข้องอยู่แล้ว จึงเลือก "ไม่จำเป็น" ไม่ได้ — เลือกได้จริงแค่ **2–5**
   - ต้องมีข้อความอธิบายกำกับว่าทำไมกดระดับ 1 ไม่ได้ (helper text หรือ `title`/`aria-describedby`) ไม่ใช่ปล่อยให้เทา ๆ เฉย ๆ
5. ถ้าติ๊กแล้วไม่เลือกระดับ → ถือว่ายังกรอกไม่ครบ (validate ตอน submit เท่านั้น ตามกติกาโปรเจกต์)
6. ถ้าติ๊กออก → ล้างค่าระดับของข้อนั้นทิ้ง

### ป้ายระดับความจำเป็น

| ค่า | ไทย | อังกฤษ (เสนอ — รอยืนยัน) | สถานะ |
|---|---|---|---|
| 1 | ไม่จำเป็น / ไม่เกี่ยวข้อง | Not required / Not relevant | **disabled ตลอด** |
| 2 | จำเป็นเล็กน้อย | Slightly required | เลือกได้ |
| 3 | จำเป็นปานกลาง | Moderately required | เลือกได้ |
| 4 | จำเป็นมาก | Highly required | เลือกได้ |
| 5 | จำเป็นมากที่สุด | Most required | เลือกได้ |

### รายการทักษะ 16 ข้อ

| # | ไทย | อังกฤษ |
|---|---|---|
| 1 | ภาวะผู้นำและผู้เปลี่ยนแปลงทางสังคม | Leadership and Social Change |
| 2 | การทำงานร่วมกับผู้อื่น | Working with others |
| 3 | การคิดวิเคราะห์และการคิดอย่างมีวิจารณญาณ | Analytical and Critical Thinking |
| 4 | การแก้ไขปัญหาเชิงซับซ้อน | Solving complex problems |
| 5 | การสื่อสารอย่างมีประสิทธิภาพ | Effective communication |
| 6 | ความคิดสร้างสรรค์ | Creativity |
| 7 | ความซื่อสัตย์ | Honesty |
| 8 | ความกตัญญู | Gratitude |
| 9 | ทักษะดิจิทัล | Digital Skills |
| 10 | มีวินัย | Disciplined |
| 11 | ใจอาสา | Volunteer |
| 12 | การเป็นผู้ประกอบการ | Entrepreneurship |
| 13 | ทักษะใฝ่เรียนรู้ | Learning Skills |
| 14 | มีสุขภาวะ | Have good health |
| 15 | ทักษะภาษาอังกฤษ | English Skills |
| 16 | แนวคิดแบบเติบโต | Growth Mindset |

> **หมายเหตุ:** ต้นฉบับที่ได้รับสะกดข้อ 16 ว่า "แรวคิดแบบเติบโต" — แก้เป็น "**แนวคิดแบบเติบโต**" ให้ตรงกับ Growth Mindset ถ้าต้นฉบับทางการสะกดแบบเดิมจริง ให้แจ้งกลับ

### การนำคะแนนไปใช้

**เก็บเป็นข้อมูลเชิงสถิติเท่านั้น — ไม่รวมในคะแนนนักศึกษา**
เป็นข้อมูลว่า *สถานประกอบการคาดหวังอะไร* ไม่ใช่ *นักศึกษาทำได้แค่ไหน* จึงต้อง:

- ไม่เข้า `lo_score` / `lo_count` / `c_score`
- ไม่ขึ้นในหน้าสรุปผลว่าเป็นคะแนนของนักศึกษา
- สถานะ "ตอบครบ" ของส่วนนี้นับแยกใน `stepCompletion`

### งานเชิงเทคนิค

- **คอมโพเนนต์ใหม่** `web/src/components/evaluation/SkillExpectationGroup.tsx`
  - ใช้ `RatingCard` ซ้ำสำหรับสเกล 1–5 ได้ แต่ `RatingCard` **ยังไม่รองรับ `disabled` รายระดับ** → ต้องเพิ่ม prop `disabledValues?: number[]` และเลี่ยงไม่ให้ลูกศร ←/→ โฟกัสไปที่ระดับที่ disabled (ดู `RatingCard.tsx` `focusCard`)
  - ต้อง sync ค่าจาก DOM ผ่าน `formVersion` แบบเดียวกับ `CompetencyStep` / `ReportStep` เพราะฟอร์มเป็น uncontrolled + มี draft restore
- **ชื่อฟิลด์** เสนอ `skill-{n}` (checkbox, value=`"1"`) และ `skill-{n}-level` (radio, value=`2..5`) โดย `n` = 1..16
- **Schema** เพิ่ม `skillExpectationStepSchema` ใน `web/src/lib/evaluation-schema.ts`
  - กติกา: ต้องมีอย่างน้อย 1 ข้อที่ติ๊ก; ทุกข้อที่ติ๊กต้องมี level อยู่ในช่วง 2–4? → **2–5**
  - error key ใหม่: `skills_min_one`, `skills_level_required` + ข้อความ th/en ใน `ERROR_MESSAGES`
- **การเก็บข้อมูล** `payload_json` เก็บอัตโนมัติอยู่แล้ว แต่ถ้าจะทำรายงานสถิติควรเพิ่มตารางแยก
  → ดู [คำถามค้าง Q3](#q3--รูปแบบการเก็บข้อมูลส่วนที่-2)
- **`recomputeCompletion` / `hasMinimumData`** ใน `EvaluationWizard.tsx` ต้องเพิ่มเคสของ step ใหม่
- **`fieldStep()`** ต้อง map ฟิลด์ `skill-*` ไปยัง step ที่ถูกต้อง (ใช้ตอน error summary เด้งไปหน้าที่ผิด)

### เกณฑ์ผ่าน

- [ ] ยังไม่ติ๊กอะไร → กดถัดไปได้ (กติกา "ห้าม block navigation" ยังต้องอยู่)
- [ ] กด submit โดยไม่ติ๊กเลย → error "เลือกอย่างน้อย 1 ทักษะ" และเด้งกลับมาส่วนที่ 2
- [ ] ติ๊กแล้วเห็นสเกลเด้งออกมาทันที
- [ ] ระดับ 1 กดไม่ได้ทั้งเมาส์และคีย์บอร์ด และมีคำอธิบายว่าทำไม
- [ ] ติ๊กออก → ค่าระดับหาย ไม่ค้างใน payload
- [ ] บันทึกร่าง (autosave) แล้วรีเฟรช → ทั้ง checkbox และระดับกลับมาครบ
- [ ] โหมด `en` ไม่มีตัวอักษรไทยปน โหมด `th` ไม่มีอักษรละตินปน

---

## G3 — rubric ของ LOs: ตัดป้ายระดับออก เหลือ เลข + คำอธิบาย

> ✅ **เสร็จแล้ว** (commit 72755ca) — RatingCard.tsx แก้แล้ว, fallback กรณีไม่มี description ทำงานถูกต้อง (แยก th/en อิสระ).
### สิ่งที่ต้องได้

การ์ดคะแนนของคำถาม LO ปัจจุบันแสดง 3 บรรทัด: `เลข` / `ป้าย (ดีมาก/ดี/พอใช้/ต้องปรับปรุง)` / `คำอธิบาย rubric`
→ **ตัดบรรทัดป้ายออก เหลือ `เลข` + `คำอธิบาย`**

ตัวอย่างข้อที่ต้องเปลี่ยน (หลักสูตรศิลปะการประกอบอาชีพ ระดับ 1):

```
ก่อน:  1 / ต้องปรับปรุง / การจัดการวัตถุดิบ: ไม่สามารถจัดการวัตถุดิบได้อย่างเหมาะสม…
หลัง:  1 / การจัดการวัตถุดิบ: ไม่สามารถจัดการวัตถุดิบได้อย่างเหมาะสม…
```

### กติกา fallback (ยืนยันแล้ว)

**ข้อที่ไม่มีคำอธิบายในฐานข้อมูล → คงป้ายเดิมไว้** (ไม่ปล่อยให้การ์ดเหลือแต่ตัวเลขโดด ๆ)

| เงื่อนไข | แสดง |
|---|---|
| มี `description` | `เลข` + `คำอธิบาย` |
| ไม่มี `description` | `เลข` + `ป้าย` (เหมือนเดิม) |

### งานเชิงเทคนิค

- `web/src/components/evaluation/RatingCard.tsx` — แก้ตรงที่ render `level.label` ให้แสดงเฉพาะเมื่อ `!level.description`
- **ห้ามให้กระทบส่วนรายงาน** — ส่วนรายงานไม่มี `description` อยู่แล้ว จึงเข้าเคส fallback และแสดงป้ายต่อไปโดยอัตโนมัติ ✅ ตรงตามที่ต้องการใน [G4](#g4--รายงานโครงงาน-ลดจาก-5-ระดับเหลือ-4-ระดับ)
- ตรวจ `description_th` / `description_en` ทั้งคู่ — ถ้า th มีแต่ en ว่าง โหมด en จะกลายเป็นการ์ดเปล่า ต้อง fallback เป็นป้าย en

### เกณฑ์ผ่าน

- [ ] หลักสูตรที่มี rubric ครบ (เช่น CUL, MARSCI, LOG) → ไม่เห็นคำว่า ดีมาก/ดี/พอใช้/ต้องปรับปรุง ในการ์ด LO
- [ ] หลักสูตรที่ยังไม่มี rubric → ยังเห็นป้ายเหมือนเดิม ไม่มีการ์ดเลขโดด
- [ ] สลับเป็นโหมด `en` แล้วไม่มีการ์ดที่ว่างเปล่า
- [ ] ส่วนรายงาน/โครงงาน ยังแสดงป้าย ดีมาก/ดี/พอใช้/ต้องปรับปรุง ครบ

---

## G4 — รายงาน/โครงงาน: ลดจาก 5 ระดับเหลือ 4 ระดับ

> ✅ **โค้ดเสร็จแล้ว** (commits 48e17da บริษัท + af59a57 เดิมของอาจารย์นิเทศ) — verified สดใน browser: ส่งจริงได้คะแนนรายงาน 20/20 (ไม่ใช่ 25), หน้าสรุปผลหารด้วย 20 ถูกต้อง. `grep -rn "adv5ScoreSchema\|rating5" web/src` ไม่เจอ. CONTEXT.md แก้กติกาแล้ว. ⚠️ **migration 019 ยังไม่ได้รันจริงกับ DB production** — dry-run ยืนยันแล้วว่าปลอดภัย (0 แถว company, 1 แถว advisor 25→20 คงเปอร์เซ็นต์เดิม) แต่ auto-mode classifier ปฏิเสธไม่ให้รันจริง ต้องให้ผู้ใช้รันเองในเทอร์มินัล.

### สิ่งที่ต้องได้

| ค่า | ไทย | อังกฤษ (เสนอ) |
|---|---|---|
| 4 | ดีมาก | Very good |
| 3 | ดี | Good |
| 2 | พอใช้ | Fair |
| 1 | ต้องปรับปรุง | Needs improvement |

แทนสเกลเดิม `น้อยที่สุด / น้อย / ปานกลาง / มาก / มากที่สุด` (5 ระดับ)

### ขอบเขต — ยืนยันแล้ว: ทั้งสองฝั่ง

"ทั้งระบบ" = **รวมฝั่งอาจารย์นิเทศด้วย** (ยืนยัน 2026-09-11) ชุดรายงานที่ต้องเปลี่ยนมี 2 ชุด:

| ชุด | ฟิลด์ | ไฟล์ schema | ปัจจุบัน |
|---|---|---|---|
| สถานประกอบการ | `c-0` … `c-4` | `web/src/lib/evaluation-schema.ts` → `reportItemSchema` | 1–5 |
| อาจารย์นิเทศ | `adv-report-0` … `adv-report-4` | `web/src/lib/advisor-schema.ts` → `adv5ScoreSchema` | 1–5 |

**เฉพาะสเกลรายงานเท่านั้นที่แตะฝั่งอาจารย์นิเทศ** — โครงสร้างส่วนอื่นของฟอร์มอาจารย์นิเทศ (ส่วนที่ 2 ศูนย์สหกิจฯ, ส่วนที่ 3 สถานประกอบการ, ด้านอื่น ๆ) ยังคงเดิมทุกอย่างใน phase นี้

หลังเปลี่ยนเสร็จ `adv5ScoreSchema` จะไม่มีใครเรียกใช้แล้ว → **ลบทิ้ง** ไม่ใช่ปล่อยค้างไว้ (ไม่งั้น phase หน้าจะมีคนหยิบไปใช้ผิด)

### งานเชิงเทคนิค

- `web/src/lib/evaluation-schema.ts`
  - `reportItemSchema` — ช่วง `1..5` → `1..4`
  - คะแนนเต็มส่วนรายงาน `5 ข้อ × 5 = 25` → `5 ข้อ × 4 = 20`
- `web/src/components/evaluation/copy.ts` — `rating: [...]` ของส่วนรายงาน เปลี่ยนเป็น 4 ป้ายใหม่ (**ระวัง:** ตัวแปร `rating` ตัวนี้ถูกใช้เป็น fallback ของคำถาม LO ที่ไม่มี options ใน DB ด้วย — ต้องแยกเป็น `reportRating` คนละตัวก่อน ไม่งั้นจะไปเปลี่ยนสเกล LO โดยไม่ตั้งใจ)
- `web/src/components/evaluation/steps.tsx` → `ReportStep` — ตัวแปร `levels` สร้างจาก `copy.rating` ต้องชี้ไปที่ชุดใหม่
- `web/src/app/programs/[programId]/actions.ts` — guard `score < 1 || score > 5` สำหรับคีย์ `c-*` → `> 4`
- `web/src/components/evaluation/CompletionScreen.tsx` — **hardcode `cCount * 5`** เป็นตัวหารของคะแนนรายงานในหน้าสรุปผล → `* 4` (ถ้าลืมจุดนี้ หน้าขอบคุณจะโชว์ `16 / 25` ทั้งที่เต็ม 20)
- `web/src/components/advisor/advisor-copy.ts` — `rating5` ไม่มีใครใช้แล้ว → ลบทิ้ง, ให้ `reportItems` ใช้ `rating4`
- `web/src/lib/advisor-schema.ts` — `adv5ScoreSchema` → ใช้ `adv4ScoreSchema` แทนใน `advisorReportStepSchema` แล้วลบตัวเก่าทิ้ง
- `web/src/app/[school]/[program]/advisor/actions.ts` + `AdvisorCompletionScreen.tsx` — ตรวจ guard ช่วงคะแนนและตัวหารของ `reportScore` แบบเดียวกับฝั่งสถานประกอบการ
- `CONTEXT.md` — **ต้องแก้กติกาที่ล็อกไว้** ปัจจุบันเขียนว่า "ใช้ 4 ระดับทุกรายการ ยกเว้นรายงานที่ใช้ 5 ระดับ ห้ามใช้ 5 ระดับกับรายการอื่น" → กลายเป็น **4 ระดับทั้งหมด ไม่มีข้อยกเว้น**
- คอมเมนต์ที่เขียนกำกับว่า "อย่าขยายเป็น 5" / "only the report is 5-level" ใน `advisor-schema.ts` และ `advisor-copy.ts` ต้องแก้ให้ตรงความจริงใหม่ — ตอนนี้เขียนล็อกไว้แข็งมาก ("this split is an explicit, locked project rule, not an oversight") ถ้าไม่แก้จะทำให้คนอ่านทีหลังเข้าใจผิด

### การแปลงข้อมูลเดิม (ยืนยันแล้ว: แปลงโดยคงสัดส่วนเปอร์เซ็นต์)

ข้อมูลที่ส่งเข้ามาแล้วเก็บ `c_score` เต็ม 25 → ต้องแปลงเป็นฐาน 20 โดยคงเปอร์เซ็นต์เดิม

```
c_score_ใหม่ = round(c_score_เดิม / 25 × 20)
```

ตัวอย่าง: `20/25 = 80%` → `16/20 = 80%` ✅

- เขียน migration `migrations/019_convert_report_scale_5_to_4.mjs`
- ต้องเก็บค่าดิบเดิมไว้กู้คืนได้ — เพิ่มคอลัมน์ `c_score_legacy` หรือ `scale_version` ใน `evaluation_submissions` ก่อนเขียนทับ
- `payload_json` เก็บค่ารายข้อ (`c-0`..`c-4`) เป็น 1–5 → ตัดสินใจว่าจะแปลงรายข้อด้วยหรือเก็บไว้เป็นหลักฐาน (**เสนอ: เก็บ payload เดิมไว้ไม่แตะ แปลงเฉพาะคอลัมน์คะแนนสรุป**)
- ต้องนับก่อนว่ามี submission จริงกี่รายการ — ถ้าเป็น 0 ก็ข้าม migration ได้ ประหยัดเวลามาก

### เกณฑ์ผ่าน

- [ ] ส่วนรายงาน **ทั้งสองฟอร์ม** แสดง 4 การ์ด เรียง 4→1 (มากไปน้อย ตามแบบเดิม)
- [ ] ป้ายตรงตามตาราง ดีมาก/ดี/พอใช้/ต้องปรับปรุง
- [ ] คำถาม LO ที่ไม่มี options ใน DB **ไม่ถูกเปลี่ยนสเกลโดยไม่ตั้งใจ**
- [ ] submit ค่า 5 ในช่อง `c-*` และ `adv-report-*` → server ปฏิเสธทั้งคู่
- [ ] หน้าสรุปผลทั้งสองฟอร์มแสดงตัวหารเป็น 20 ไม่ใช่ 25
- [ ] คะแนนเดิมที่แปลงแล้ว เปอร์เซ็นต์ตรงกับของเดิม (ยกเว้นการปัดเศษ)
- [ ] `grep -rn "adv5ScoreSchema\|rating5" web/src` ไม่เจอผลลัพธ์
- [ ] `CONTEXT.md` ไม่มีกติกา "รายงานใช้ 5 ระดับ" หลงเหลือ

---

## G5 — ย้ายและแปลงส่วนประเมินกระบวนการศูนย์สหกิจศึกษาฯ

> ✅ **เสร็จแล้ว** (commit f207169) — verified สดใน browser: ไม่มีส่วนที่ 7 แยกแล้ว, ลำดับส่วนข้อเสนอแนะถูกต้อง, "สมรรถนะที่คาดหวัง" ตัดออกแล้ว, center_score/center_count แยกจาก c_score จริง (verified: submit จริงได้ centerScore 8/8 แยกจาก cScore 20/20), ข้อความ/สเกลตรงกับอาจารย์นิเทศเป๊ะ (ใช้ lib/coop-center-copy.ts ร่วมกัน).

### สิ่งที่ต้องได้

ส่วนที่ 6 เดิม (`ProcessStep`) ถูก **ยุบทิ้งทั้งส่วน** เนื้อหาย้ายไปต่อท้ายส่วนข้อเสนอแนะ (ส่วนที่ 5 เดิม) ถัดจากคำถาม *"ปีการศึกษาหน้าต้องการรับนักศึกษากี่คน"*

| ของเดิมในส่วนที่ 6 | ชะตากรรม |
|---|---|
| `process_evaluation` (ปลายเปิด "ประเมินกระบวนการดำเนินงานสหกิจศึกษา") | **แปลงเป็นคำถามให้คะแนน 2 ข้อ** ย้ายไปท้ายส่วนข้อเสนอแนะ |
| `expected_competencies` ("สมรรถนะที่คาดหวังจากนักศึกษาสหกิจศึกษา") | **ตัดทิ้ง** |
| `other_comments` ("ข้อคิดเห็นอื่น ๆ เพิ่มเติม") | **ย้าย** ไปต่อท้ายสุด ถัดจากคำถามข้อ 2 |
| กล่องสรุป "ตรวจสอบก่อนส่ง" + `processNotice` | ย้ายไปท้ายส่วนข้อเสนอแนะด้วย (เป็นส่วนสุดท้ายของฟอร์มแล้ว) |

### คำถามใหม่ 2 ข้อ

หัวข้อกลุ่ม: **ประเมินกระบวนการดำเนินงานของศูนย์สหกิจศึกษาและพัฒนาอาชีพ**

1. การดำเนินงานของศูนย์สหกิจศึกษาฯ เช่น การประสานงานกับสถานประกอบการ การคัดเลือกนักศึกษา การนัดหมายนิเทศงาน โดยใช้เวลาเหมาะสมและเพียงพอ และสื่อสารชัดเจน \*
2. ความสะดวกรวดเร็ว ความพร้อมในการประสานงานและการบริการของศูนย์สหกิจศึกษาฯ \*

สเกล 4 ระดับ: `4 ดีเยี่ยม` / `3 ดี` / `2 พอใช้` / `1 ควรปรับปรุง`

### ข้อสังเกตสำคัญ — ใช้ข้อความซ้ำได้เลย

ฝั่งอาจารย์นิเทศมี **คำถาม 2 ข้อนี้เป๊ะ ๆ อยู่แล้ว** ที่ `web/src/components/advisor/advisor-copy.ts` → `centerTitle` + `centerItems` พร้อมคำแปลอังกฤษที่ล็อกไว้เมื่อ 2026-09-07:

```
"Cooperative education procedures (i.e. matching, work site visit arrangement and communication)"
"The convenience, promptness, and readiness of the Center for Cooperative Education and Career Development staff"
```

และ `rating4: ["ควรปรับปรุง", "พอใช้", "ดี", "ดีเยี่ยม"]` ก็ตรงกับที่ต้องการทุกประการ

→ **ควรยกข้อความชุดนี้ขึ้นเป็นโมดูลกลางที่ทั้งสองฟอร์มใช้ร่วมกัน** (เสนอ `web/src/lib/coop-center-copy.ts`) ไม่ใช่ก๊อปวาง เพราะเป็นคำถามชุดเดียวกันที่ถามคนละกลุ่มผู้ตอบ ถ้าแยกกันเก็บจะ drift แน่นอน

> **ไม่ใช่ความซ้ำซ้อนที่ผิด** — ทั้งสถานประกอบการและอาจารย์นิเทศต่างประเมินศูนย์สหกิจฯ ด้วยเกณฑ์เดียวกัน เป็นการเก็บ 2 มุมมอง ตั้งใจให้ซ้ำ

### ⚠️ ป้ายระดับไม่สอดคล้องกันทั้งระบบ

ตอนนี้จะมีสเกล 4 ระดับ 2 ชุดที่ป้ายไม่ตรงกัน:

| ใช้ที่ไหน | 4 | 3 | 2 | 1 |
|---|---|---|---|---|
| รายงาน/โครงงาน ([G4](#g4--รายงานโครงงาน-ลดจาก-5-ระดับเหลือ-4-ระดับ)) | ดีมาก | ดี | พอใช้ | **ต้องปรับปรุง** |
| กระบวนการศูนย์สหกิจฯ (G5) | **ดีเยี่ยม** | ดี | พอใช้ | **ควรปรับปรุง** |

ต้นฉบับ feedback ระบุมาแบบนี้จริง จึงทำตามก่อน แต่ควรถามว่าตั้งใจให้ต่างกันไหม (ดู [คำถามค้าง Q4](#q4--ป้ายระดับ-4-3-2-1-ที่ไม่ตรงกันระหว่างสองชุด))

### งานเชิงเทคนิค

- ลบ `ProcessStep` ออกจาก `web/src/components/evaluation/steps.tsx`
- ขยาย `FeedbackStep` ให้รับคำถามคะแนน 2 ข้อ + `other_comments` + กล่องสรุป
- `web/src/components/evaluation/copy.ts` — ลบ `expectedCompetencies`; เพิ่มหัวข้อกลุ่ม + 2 ข้อคำถาม (หรือ import จากโมดูลกลาง)
- `web/src/lib/evaluation-schema.ts` — `feedbackStepSchema` เพิ่ม `center-0`, `center-1` (บังคับ ช่วง 1–4) และรับ `other_comments` แบบ optional
- `web/src/app/programs/[programId]/actions.ts` — คะแนน 2 ข้อนี้ **ไม่เข้า `lo_score`/`c_score`** ต้องเก็บแยก (เสนอคอลัมน์ `center_score`, `center_count` แบบเดียวกับที่ฝั่งอาจารย์นิเทศทำ)
- `EvaluationWizard.tsx` — ตัดเงื่อนไข `if (i === 5) continue;` ตอน submit (เดิมข้าม step 6 เพราะ optional) และแก้ `hasMinimumData` / `fieldStep` ให้ตรงโครงสร้างใหม่

### เกณฑ์ผ่าน

- [ ] ไม่มีส่วนที่ 7 / ไม่มี step "กระบวนการ" แยกอีกต่อไป
- [ ] ส่วนข้อเสนอแนะเรียงลำดับ: จุดเด่น → ข้อควรปรับปรุง → สนใจรับเข้าทำงาน → ปีต่อไปยินดีรับ → จำนวนที่รับ → **ประเมินศูนย์สหกิจฯ ข้อ 1 → ข้อ 2 → ข้อคิดเห็นอื่น ๆ**
- [ ] ไม่มีช่อง "สมรรถนะที่คาดหวังจากนักศึกษาสหกิจศึกษา" เหลืออยู่
- [ ] คะแนน 2 ข้อนี้ไม่โผล่ในคะแนนนักศึกษาที่หน้าสรุปผล
- [ ] ข้อความไทย/อังกฤษของ 2 ข้อนี้ตรงกับฝั่งอาจารย์นิเทศแบบตัวต่อตัว

---

## G6 — โครงสร้างใหม่ 6 ส่วน ฝั่งสถานประกอบการ

> ✅ **เสร็จแล้ว** (commit f207169) — verified สดใน browser: StepProgressBar แสดง 6 ส่วนถูกต้องทั้ง th/en, คลิกข้ามส่วนได้อิสระ, submit ว่างเปล่าเด้งกลับส่วนที่ 1 พร้อม error ครบ 33 ข้อ (แก้บั๊ก native-required ที่เจอระหว่างทดสอบ — ดู commit). ร่างเก่าโครงสร้างเดิม: ไม่ได้ทดสอบ manual กับร่างจริงที่บันทึกไว้ก่อนหน้า (ยังไม่มีร่างเก่าแบบนั้นอยู่ใน DB ให้ทดสอบ) แต่ clamp logic (`Math.min(draft.currentStep, steps.length-1)`) ป้องกัน crash แน่นอน.
### ผลลัพธ์สุดท้าย

| ส่วน | ชื่อ | ที่มา |
|---|---|---|
| 1 | ข้อมูลทั่วไป | เดิม (ไม่เปลี่ยน) |
| 2 | **ทักษะที่สถานประกอบการคาดหวังจากนักศึกษา** | **ใหม่** ([G2](#g2--ส่วนที่-2-ใหม่-ทักษะที่สถานประกอบการคาดหวังจากนักศึกษา)) |
| 3 | ความรู้และทักษะ | เดิมส่วนที่ 2 |
| 4 | จริยธรรมและบุคลิก | เดิมส่วนที่ 3 |
| 5 | รายงาน/โครงงาน | เดิมส่วนที่ 4 (สเกลเปลี่ยน — [G4](#g4--รายงานโครงงาน-ลดจาก-5-ระดับเหลือ-4-ระดับ)) |
| 6 | ข้อเสนอแนะ | เดิมส่วนที่ 5 + เนื้อหาจากส่วนที่ 6 เดิม ([G5](#g5--ย้ายและแปลงส่วนประเมินกระบวนการศูนย์สหกิจศึกษาฯ)) |

### ตารางย้าย index (ระวังพังตรงนี้)

`EvaluationWizard.tsx` **ใช้เลข step เป็นค่าคงที่ hard-code** อยู่หลายจุด ทุกจุดต้องเลื่อน +1 จาก step 1 เป็นต้นไป:

| index เดิม | index ใหม่ | คอมโพเนนต์ |
|---|---|---|
| 0 | 0 | `GeneralStep` |
| — | **1** | `SkillExpectationStep` ← ใหม่ |
| 1 | 2 | `CompetencyStep` (primary) |
| 2 | 3 | `CompetencyStep` (secondary) |
| 3 | 4 | `ReportStep` |
| 4 | 5 | `FeedbackStep` (ขยาย) |
| 5 | — | `ProcessStep` ← ลบ |

จุดที่มีเลข step ฝังอยู่ใน `web/src/app/programs/[programId]/EvaluationWizard.tsx`:

- `hasMinimumData()` — `step === 0` / `1` / `2` / `3` / `4` / `5` (~บรรทัด 73–89)
- `validateStep()` — `step === 0` … `step === 4` (~บรรทัด 218–234)
- `handleSubmit()` — `if (i === 5) continue;` (~บรรทัด 303)
- ส่วน render — `hidden={currentStep !== N}` ทั้ง 6 บล็อก (~บรรทัด 410–430)
- `fieldStep()` — ฟังก์ชัน map ชื่อฟิลด์ → เลข step
- `copy.steps` ใน `web/src/components/evaluation/copy.ts` — array 6 รายการ (ทั้ง th และ en) ต้องแทรกรายการใหม่ที่ index 1 และลบรายการสุดท้าย

> **ข้อเสนอเชิงโครงสร้าง:** ควรถือโอกาสนี้เปลี่ยนจากเลข index เป็น **ค่าคงที่แบบมีชื่อ** (`const STEP = { GENERAL: 0, SKILLS: 1, ... }`) หรือ array ของ step descriptor ที่ประกาศครั้งเดียว — ไม่งั้น phase หน้าที่แทรกส่วนอีกจะเจอบั๊กชุดเดิมซ้ำ งานเพิ่มไม่เกิน 1 ชั่วโมง แต่กัน off-by-one ได้ทั้งหมด

### เกณฑ์ผ่าน

- [ ] แถบความคืบหน้า (`StepProgressBar`) แสดง 6 ส่วนชื่อถูกต้องทั้ง th และ en
- [ ] คลิกข้ามไปส่วนไหนก็ได้โดยไม่มี validation ขวาง (กติกาโปรเจกต์ข้อ "ห้าม gate navigation")
- [ ] submit แล้ว error เด้งกลับไปส่วนที่ถูกต้อง (ทดสอบทุกส่วน)
- [ ] บันทึกร่างที่ส่วนที่ 4 แล้วเปิดใหม่ → กลับมาที่ส่วนที่ 4 ไม่ใช่ส่วนอื่น
- [ ] ร่างเก่าที่บันทึกด้วยโครงสร้าง 6 ส่วนแบบเดิม เปิดแล้วไม่พัง (`currentStep` ที่เก็บไว้อาจชี้ผิดส่วน — ต้องมี guard)

---

## G7 — คะแนนถ่วงน้ำหนัก (LOs 35% / รายงาน 25% / อาจารย์นิเทศ 40%) — บันทึกไว้ ยังไม่ทำ

### ข้อมูลที่ได้รับ

| องค์ประกอบ | น้ำหนัก | แหล่ง |
|---|---|---|
| LOs (สมรรถนะ) | 35% | ฝั่งสถานประกอบการ |
| รายงาน/โครงงาน | 25% | ฝั่งสถานประกอบการ |
| **รวมฝั่งสถานประกอบการ** | **60%** | |
| ฝั่งอาจารย์นิเทศ | 40% | ฝั่งอาจารย์นิเทศ |

### ขอบเขต phase นี้

**เก็บตัวเลขดิบต่อไปเหมือนเดิม ยังไม่คำนวณคะแนนรวมถ่วงน้ำหนัก**

ระบบปัจจุบันเก็บ `lo_score / lo_count / lo_max` และ `c_score / c_count` เป็นผลรวมดิบใน `evaluation_submissions` ไม่มีการถ่วงน้ำหนักใด ๆ — ให้คงไว้แบบนั้น

### สิ่งที่ต้องเตรียมไว้ให้ phase หน้าทำได้ง่าย

- เก็บ **ตัวหาร** ไว้ด้วยเสมอ ไม่ใช่แค่ตัวตั้ง — `lo_max` มีแล้ว แต่ `c_score` ยังไม่มีคู่ของมัน ควรเก็บ `c_max` (= 20 หลัง [G4](#g4--รายงานโครงงาน-ลดจาก-5-ระดับเหลือ-4-ระดับ)) ลงคอลัมน์จริง ไม่ใช่ hard-code ตอนคำนวณ — ไม่งั้นเปลี่ยนสเกลอีกครั้งจะคำนวณย้อนหลังไม่ได้
- เก็บ `scale_version` ต่อ submission เพื่อให้รู้ว่าแถวนั้นใช้เกณฑ์ไหน
- ต้องมีวิธีจับคู่ submission ฝั่งสถานประกอบการกับฝั่งอาจารย์นิเทศของนักศึกษาคนเดียวกัน — ตอนนี้จับคู่ได้แค่ผ่าน `student_code` ใน `payload_json` ซึ่งไม่มี index และไม่มีข้อบังคับความถูกต้อง **เรื่องนี้จะเป็นคอขวดของ phase หน้า ควรเริ่มคิดตั้งแต่ตอนนี้**

---

## G11 — ย่อคำอธิบาย rubric ที่ยาวเกินจำเป็น

> **ผู้รับผิดชอบ: Claude (ไม่ใช่ agent)** — เป็นงานบรรณาธิกรที่ต้องใช้วิจารณญาณรายข้อ ไม่ใช่งานที่ automate ได้
> **สำรวจจากฐานข้อมูลจริงแล้วเมื่อ 2026-09-11**

### ปัญหา

คำอธิบาย rubric แต่ละระดับ **เอาโจทย์ทั้งประโยคมาเขียนซ้ำ** แล้วต่อท้ายด้วยส่วนที่ต่างกันจริง ๆ เพียงไม่กี่คำ
ผลคือการ์ดคะแนนยาวจนอ่านไม่ไหว และผู้ประเมินต้องกวาดตาหาความต่างเอง

### ตัวเลขจากฐานข้อมูลจริง

| ตัวชี้วัด | ค่า |
|---|---|
| คำถามที่มี rubric | **232 ข้อ** |
| ตัวเลือกที่มีคำอธิบาย | **928 รายการ** |
| ตัวอักษรรวมใน rubric | **101,838** |
| ส่วนที่ซ้ำกันทุกระดับของข้อเดียวกัน | **~27,740 (27%)** |
| rubric ที่ยาวเกิน 150 ตัวอักษร | **80 ข้อ** |
| rubric ที่ยาวเกิน 250 ตัวอักษร | **11 ข้อ** |

**หลักสูตรที่หนักสุด:** MARSCI (ตัดได้ ~6,032), CIVIL (~5,020), SCI (~3,592), EE (~2,964)
ยาวที่สุดคือ MARSCI PLO8 ที่ **284 ตัวอักษรในการ์ดเดียว**

### หลักการแก้ (ยึดตามตัวอย่างที่ผู้ใช้ให้มา)

> **สิ่งเดียวที่มีค่าในคำอธิบายระดับหนึ่ง คือสิ่งที่ทำให้มันต่างจากระดับอื่น**
> ส่วนที่ซ้ำกันทุกระดับ = โจทย์ ซึ่งผู้ประเมินอ่านไปแล้วบรรทัดบน ไม่ต้องเขียนซ้ำ

1. หาส่วนที่ปรากฏเหมือนกันในทุกระดับของข้อนั้น
2. **ย่อ** ให้เหลือวลีสั้นที่ยังอ่านรู้เรื่อง — **ไม่ใช่ลบทิ้งทั้งก้อน** (ลบดื้อ ๆ จะได้ประโยคไทยที่พัง เช่น "สามารถได้ดี ผลงานมีความสมบูรณ์")
3. เก็บส่วนที่แยกระดับไว้ให้ครบ ห้ามตัด
4. ห้ามเปลี่ยนความหมายหรือระดับความเข้มของเกณฑ์

### ตัวอย่าง (THAI LO1 — ตรงกับที่ผู้ใช้ยกมา)

| ระดับ | ก่อน (240–246 ตัวอักษร) | หลัง (~70–110) |
|---|---|---|
| 4 | สามารถประยุกต์ใช้องค์ความรู้**ด้านมนุษยศาสตร์ สังคมศาสตร์ ด้านภาษา วรรณกรรม และวัฒนธรรมไทย เพื่อสร้างสรรค์เนื้อหาในงานเขียน งานประชาสัมพันธ์ หรือ งานวิชาการที่ได้รับมอบหมายในสถานประกอบการ**ได้ดี ผลงานมีความสมบูรณ์ และสามารถใช้งานได้จริงในองค์กร | สามารถประยุกต์ใช้องค์ความรู้เพื่อสร้างสรรค์เนื้อหาในงานได้ดี ผลงานมีความสมบูรณ์ และสามารถใช้งานได้จริงในองค์กร |
| 1 | ไม่สามารถประยุกต์ใช้องค์ความรู้**ด้านมนุษยศาสตร์ … ที่ได้รับมอบหมาย**ได้ | ไม่สามารถประยุกต์ใช้องค์ความรู้เพื่อสร้างสรรค์เนื้อหาในงานได้ |

### 🔴 ปัญหาที่เจอระหว่างสำรวจ — บาง rubric ไม่แยกระดับเลย

**CIVIL LO5 ระดับ 4** มีคำอธิบายเท่ากับโจทย์เป๊ะ ๆ ไม่มีคำบอกระดับต่อท้ายสักคำ
(ระดับ 3 มี "โดยยังขาดความสมบูรณ์ในบางส่วน", ระดับ 2 มี "โดยต้องการคำแนะนำอย่างใกล้ชิด" แต่ระดับ 4 ไม่มีอะไรเลย)

แปลว่าผู้ประเมิน **แยกไม่ออกว่า "ทำได้ระดับดีมาก" ต่างจาก "ทำได้" ยังไง** — เป็นปัญหาเชิงเนื้อหา ไม่ใช่แค่ยาว
ระหว่างย่อ ต้องไล่หาข้อแบบนี้แล้ว **เติมคำบอกระดับให้** พร้อมทำรายการแจ้งศูนย์สหกิจฯ ว่าเติมอะไรไปบ้าง

### ✅ ตัดสินแล้ว 2026-09-11 — ย่อทับของเดิม ไม่เก็บฉบับเต็มไว้ในฟอร์ม

เคยพิจารณาทางเลือก "แสดงฉบับย่อบนการ์ด + มีไอคอน `i` กดดูฉบับเต็ม" แล้ว **ตกไป**

**ข้อสรุป:** ย่อทับ `description_th` เดิมไปเลย ในฟอร์มมีฉบับย่ออย่างเดียว
→ **ไม่ต้องเพิ่มคอลัมน์ใหม่ ไม่ต้องทำ popover ไม่ต้องแตะ `RatingCard` เพิ่มจาก [G3](#g3--rubric-ของ-los-ตัดป้ายระดับออก-เหลือเลข--คำอธิบาย)**

ผลคือฉบับเต็มจะเหลืออยู่ใน 2 ที่เท่านั้น: `template_revisions` (snapshot) และไฟล์ตารางก่อน/หลังใน git
→ **การบันทึก log จึงเป็นข้อบังคับ ไม่ใช่ของแถม** เพราะเป็นทางเดียวที่จะกู้ข้อความต้นฉบับจากสำนักวิชากลับมาได้

### การบันทึกประวัติเพื่อย้อนกลับ (ผู้ใช้ขอ 2026-09-11)

**ใช้กลไกที่ระบบมีอยู่แล้ว ไม่ต้องสร้างใหม่:**

ตาราง `template_revisions` (จาก `migrations/005_editor_schema_compatibility.sql`) เก็บ `snapshot_json` ของทั้ง template
และมีหน้า `/programs/{programId}/history` พร้อมปุ่มกู้คืนให้ใช้งานผ่านเว็บอยู่แล้ว

**ขั้นตอนบังคับของ migration:**
1. **ก่อนแก้** — snapshot ทุก template ที่จะถูกแตะ ลง `template_revisions` โดยใช้ query เดียวกับ `snapshotCurrent()` ใน `web/src/app/programs/[programId]/edit/actions.ts:47` (ต้องรูปแบบเดียวกันเป๊ะ ไม่งั้นปุ่มกู้คืนจะใช้ไม่ได้)
2. `kind = 'edit'`, `note = 'ย่อคำอธิบาย rubric (G11) — <วันที่>'`
3. ทำใน transaction เดียว — snapshot ไม่สำเร็จ ห้ามแก้
4. เก็บไฟล์ `docs/rubric-rewrite-<วันที่>.md` ตาราง ก่อน/หลัง ทุกข้อไว้ใน repo ด้วย เพื่อให้ย้อนดูได้โดยไม่ต้องเปิด DB

> ผลลัพธ์: ย้อนกลับได้ 2 ทาง — ผ่านปุ่มกู้คืนในหน้าประวัติของแต่ละหลักสูตร และผ่านไฟล์ diff ใน git

**ต้องเห็นใน log การแก้ไขจริง (ผู้ใช้ย้ำ):**
หลังรัน migration แล้ว เปิด `/programs/{programId}/history` ต้องเห็นรายการใหม่โผล่ขึ้นมาพร้อม `note` ที่อ่านรู้เรื่องว่าเป็นการย่อ rubric
ไม่ใช่แค่เขียนลงตารางเงียบ ๆ — **ต้องทดสอบว่ากดปุ่มกู้คืนแล้วข้อความเต็มกลับมาจริง** อย่างน้อย 1 หลักสูตรก่อนถือว่าเสร็จ

✅ **ตรวจแล้ว: ไม่ต้องแก้ UI** — `history/page.tsx:117` แสดง `rev.note` อยู่แล้ว (ถ้าไม่มี note จะขึ้นว่า "ไม่มีรายละเอียดเพิ่มเติม")
ขอแค่เขียน `note` ให้ชัดเจนตอน insert ก็จะโผล่ในหน้าประวัติเอง

### ขั้นตอนการทำงาน

- [x] สำรวจฐานข้อมูล หาปริมาณและข้อที่หนักสุด
- [ ] **ร่างตัวอย่าง 5–6 ข้อให้ผู้ใช้ตรวจสไตล์ก่อน** ← อยู่ตรงนี้
- [ ] ร่างฉบับย่อทั้ง 80 ข้อที่ยาวเกิน 150 ตัวอักษร
- [ ] ทำไฟล์ตาราง ก่อน/หลัง ให้ศูนย์สหกิจฯ ตรวจ
- [ ] เขียน migration พร้อม snapshot (ยังไม่รัน)
- [ ] รัน migration หลังได้รับอนุมัติ

### ขอบเขต

ทำเฉพาะ **`description_th`** ก่อน — `description_en` ส่วนใหญ่ยังว่างอยู่ ถ้าจะย่อฝั่งอังกฤษต้องรอให้เนื้อหาไทยนิ่งก่อน

### เกณฑ์ผ่าน

- [ ] ไม่มี rubric ข้อไหนยาวเกิน ~120 ตัวอักษร (เว้นข้อที่จำเป็นจริง ๆ)
- [ ] ทุกระดับของข้อเดียวกัน **อ่านแล้วเห็นความต่างทันที**
- [ ] ไม่มีระดับไหนที่คำอธิบายเท่ากับโจทย์เป๊ะ
- [ ] ความหมายและความเข้มของเกณฑ์ไม่เปลี่ยน
- [ ] มี snapshot ใน `template_revisions` ครบทุก template ที่แตะ และกดกู้คืนจากหน้าประวัติได้จริง
- [ ] มีไฟล์ตาราง ก่อน/หลัง ใน repo

---

## G10 — ฟอร์มใหม่: แบบสอบถามนักศึกษาหลังกลับจากการปฏิบัติงานสหกิจศึกษา

> ✅ **เสร็จแล้ว** (commit 5ada0eb) — verified สดใน browser: เปิด th/en ได้ทั้งคู่, ส่วนที่ 3–4 ดึง LO จริงจาก DB (10 ข้อสำหรับ IMAG), เลขข้อส่วนที่ 6 เริ่มที่ N+3=13 ถูกต้อง (ไม่ hard-code), สเกล 4 ระดับทั้งฟอร์ม, disclosure ค่าตอบแทน "ไม่มี" ซ่อน+ล้างค่า 8.1–8.3 จริง, คำชี้แจงหัวฟอร์มเด่นชัด, submit จริงสำเร็จ (LO 40/40), form-picker แสดง 3 ตัวเลือก. ยังไม่ได้ทดสอบเปิด**คนละหลักสูตร**เพื่อยืนยันว่าเลขข้อขยับตามจริง (ทดสอบแค่หลักสูตรเดียว) — กลไกคำนวณจาก `loQuestionCount` ไม่ hard-code แน่นอนจากโค้ด แต่ไม่ได้ cross-check ด้วยหลักสูตรที่สอง.
> **แหล่งข้อมูล:** `Coop Evaluation Form _ ระบบแบบประเมิน ศูนย์สหกิจศึกษา ม.วลัยลักษณ์.pdf` (7 หน้า, 33 ข้อ)
> **นี่คือฟอร์มที่ 3 ของระบบ** — เดิมมีแค่ฝั่งสถานประกอบการกับฝั่งอาจารย์นิเทศ

### ตัวตนของฟอร์ม

| รายการ | ค่า |
|---|---|
| ชื่อเต็ม | **แบบสอบถามนักศึกษาหลังกลับจากการปฏิบัติงานสหกิจศึกษา** |
| ผู้กรอก | นักศึกษา (ประเมินตนเอง) |
| `role` | `student` |
| URL | `/{schoolSlug}/{programKey}/student/{locale}` |
| คำชี้แจงหัวฟอร์ม | "กรุณาให้ข้อมูลตามความเป็นจริง ข้อมูลนี้ไม่มีผลต่อการประเมินผลปฏิบัติงาน และใช้เพื่อพัฒนาการจัดสหกิจศึกษาเท่านั้น" |

> ⚠️ คำชี้แจงข้างบนสำคัญมาก — ต้องแสดงเด่นที่หัวฟอร์ม ไม่ใช่ซ่อนไว้ ถ้านักศึกษาคิดว่าคำตอบมีผลต่อเกรด จะได้ข้อมูลที่ไม่จริง

### การเปลี่ยนแปลงหลักจากต้นฉบับ

**ส่วนที่ 1 "การประเมินตนเอง" ในต้นฉบับเป็นคำถามตายตัว 22 ข้อ (เหมือนกันทุกหลักสูตร) → เปลี่ยนเป็น LO ของหลักสูตรนั้น ๆ แบ่งเป็นท่อนเหมือนอีก 2 ฟอร์ม**

คือ ดึงจาก `evaluation_questions` / `assessment_options` ของหลักสูตร แล้วแบ่งด้วย `PRIMARY_DOMAINS` เป็น 2 กลุ่ม (ความรู้และทักษะ / จริยธรรมและบุคลิก) — ใช้ตรรกะเดียวกับ `EvaluationWizard` และ `AdvisorWizard` เป๊ะ ๆ

**ผลที่ตามมา: จำนวนข้อไม่คงที่แล้ว** หลักสูตรหนึ่งอาจมี LO 8 ข้อ อีกหลักสูตรมี 15 ข้อ → **เลขข้อหลังส่วน LO ต้องรันใหม่ตอน render ห้าม hard-code**

### สเกลคะแนน (ยืนยันแล้ว — ต่างจากต้นฉบับ)

ต้นฉบับใช้ 1–5 (น้อยสุด…มากสุด) ทุกข้อ → **เปลี่ยนเป็น 4 ระดับทั้งฟอร์ม**

| กลุ่มคำถาม | สเกลใหม่ |
|---|---|
| LO (ส่วนที่ 3–4) | **4 ระดับตาม rubric ของ LO จาก DB** เหมือนอีก 2 ฟอร์ม (ดึงตัวเลือก+คำอธิบายจาก `assessment_options`) |
| ประเมินศูนย์สหกิจฯ + สถานประกอบการ (ส่วนที่ 6) | **4 ระดับ** — `4 ดีเยี่ยม / 3 ดี / 2 พอใช้ / 1 ควรปรับปรุง` |

เหตุผล: ทำให้คะแนนของนักศึกษา สถานประกอบการ และอาจารย์นิเทศ อยู่บนเกณฑ์เดียวกัน เทียบกันได้ตรง ๆ และตรงกับกติกา "4 ระดับทั้งระบบ" ที่ตกลงไว้ใน [G4](#g4--รายงานโครงงาน-ลดจาก-5-ระดับเหลือ-4-ระดับ)

### โครงสร้าง 6 ส่วน (ยืนยันแล้ว)

| ส่วน | ชื่อ | เนื้อหา |
|---|---|---|
| 1 | ข้อมูลทั่วไป | รหัส นศ. → ที่ตั้งหน่วยงาน |
| 2 | ค่าใช้จ่ายและที่พัก | ค่าตอบแทน/สวัสดิการ → แนะนำที่พัก |
| 3 | **ความรู้และทักษะ** | LO ชุดแรก (จาก DB) |
| 4 | **จริยธรรมและบุคลิก** | LO ชุดสอง (จาก DB) |
| 5 | จุดเด่น / สิ่งที่ต้องพัฒนา | 2 ช่องข้อความ |
| 6 | ศูนย์สหกิจฯ + สถานประกอบการ + ข้อคิดเห็น | 7 ข้อคะแนน + 2 ช่องข้อความ |

> แยกค่าใช้จ่าย/ที่พักออกจากข้อมูลทั่วไป เพราะรวมกันแล้วยาวเกิน 16 ข้อในหน้าเดียว

### รายการคำถามเต็ม

#### ส่วนที่ 1 — ข้อมูลทั่วไป

| # | คำถาม | ชนิด | บังคับ |
|---|---|---|---|
| 1 | รหัสนักศึกษา | ตัวเลข 8 หลัก | ✔ |
| 2 | ชื่อ – สกุล | ข้อความ | ✔ |
| 3 | สำนักวิชา | เลือก | ✔ |
| 4 | หลักสูตร | เลือก | ✔ |
| 5 | ภาคการศึกษา | เลือก | ✔ |
| 6 | ชื่อหน่วยงานที่ปฏิบัติงานสหกิจศึกษา | ข้อความ | ✔ |
| 7 | ที่ตั้ง / ที่อยู่ของหน่วยงาน | ข้อความ | ✔ |

> ⚠️ **ต้นฉบับ PDF มีเลขข้อ 4 ซ้ำกัน 2 ครั้ง** ("4. หลักสูตร" และ "4. ภาคการศึกษา") — เป็นบั๊กของต้นฉบับ ตารางข้างบนแก้ให้แล้ว
> `สำนักวิชา` และ `หลักสูตร` ควรเติมให้อัตโนมัติจาก URL แล้วล็อกเป็น read-only เหมือนที่ฟอร์มสถานประกอบการทำ (ดู `GeneralStep`) ไม่ใช่ให้นักศึกษาเลือกเอง — กันกรอกผิดหลักสูตรแล้วได้ชุด LO ผิด

#### ส่วนที่ 2 — ค่าใช้จ่ายและที่พัก

| # | คำถาม | ชนิด | บังคับ |
|---|---|---|---|
| 8 | ค่าตอบแทนหรือสวัสดิการ | เลือก 1: ไม่มี / เงิน / สวัสดิการ / เงินและสวัสดิการ | ✔ |
| 8.1 | ค่าตอบแทนรายวัน (บาท/วัน) | ตัวเลข (ระบุ 0 ถ้าไม่ได้รับ) | |
| 8.2 | ค่าตอบแทนรายเดือน (บาท) | ตัวเลข (ระบุ 0 ถ้าไม่ได้รับ) | |
| 8.3 | สวัสดิการที่ได้รับ | เลือกหลายข้อ: ที่พัก / อาหาร / รถรับส่ง / ค่ารักษาพยาบาล / ประกันชีวิต / ประกันอุบัติเหตุ / เครื่องแบบ + ช่อง "สวัสดิการอื่น ๆ (ถ้ามี)" | |
| 9 | ค่าที่พักโดยประมาณ **ตลอดการปฏิบัติงาน** (บาท) | ตัวเลข | |
| 10 | ค่าอาหารโดยประมาณ **ตลอดการปฏิบัติงาน** (บาท) | ตัวเลข | |
| 11 | ค่าเดินทาง **ตลอดการปฏิบัติงาน** (บาท) | ตัวเลข | |
| 12 | ค่าหนังสือและค่าธรรมเนียมอื่น ๆ (บาท) | ตัวเลข | |
| 13 | ค่าวัสดุหรือค่าประกันภัย (บาท) | ตัวเลข | |
| 14 | ค่าเช่าที่พักต่อเดือน (บาท) | ตัวเลข | |
| 15 | ค่าใช้จ่ายอื่น ๆ โปรดระบุพร้อมจำนวนเงิน | ข้อความยาว (เช่น ค่าโทรศัพท์ 1,000 บาท) | |
| 16 | ข้อมูลที่พัก (ชื่อ เบอร์โทรศัพท์ และที่ตั้ง) | ข้อความยาว — ระบุเพื่อเป็นประโยชน์แก่นักศึกษารุ่นต่อไป | |
| 17 | ท่านแนะนำที่พักนี้หรือไม่ | เลือก: แนะนำ / ไม่แนะนำ | |

> **ยืนยันแล้ว:** ต้นฉบับเขียน "รวมทั้ง 8 เดือน" ในข้อค่าที่พัก/ค่าอาหาร/ค่าเดินทาง → **ตัดเลขเดือนออก ใช้คำว่า "ตลอดการปฏิบัติงาน"** แทน กันปัญหาเวลาระยะเวลาสหกิจเปลี่ยนในอนาคต
> ควรใส่ disclosure: เลือก "ไม่มี" ในข้อ 8 → ซ่อนข้อ 8.1–8.3 (แพตเทิร์นเดียวกับ `adv_future_placement`)

#### ส่วนที่ 3–4 — LO ของหลักสูตร (จำนวนข้อไม่คงที่)

ดึงจาก DB ตามหลักสูตร แบ่ง 2 กลุ่มด้วย `PRIMARY_DOMAINS` สเกล 4 ระดับพร้อม rubric
**ทุกข้อบังคับตอบ** (ตาม `is_required` ของคำถาม เหมือนอีก 2 ฟอร์ม)

#### ส่วนที่ 5 — จุดเด่น / สิ่งที่ต้องพัฒนา

| # | คำถาม | ชนิด |
|---|---|---|
| N+1 | จุดเด่นของตนเอง | ข้อความยาว |
| N+2 | สิ่งที่ต้องการพัฒนาเพิ่มเติม | ข้อความยาว |

#### ส่วนที่ 6 — ประเมินการดำเนินงาน + การสนับสนุน + ข้อคิดเห็น

**กลุ่มย่อย: ประเมินการดำเนินงานสหกิจศึกษา** (4 ระดับ, บังคับตอบ)

| # | คำถาม |
|---|---|
| N+3 | การดำเนินการของศูนย์สหกิจศึกษา การประสานงานกับสถานประกอบการ การคัดเลือกนักศึกษา และการนัดหมายการนิเทศงาน มีความเหมาะสม เพียงพอ และสื่อสารชัดเจน |
| N+4 | ความสะดวกรวดเร็ว ความพร้อมในการประสานงานและการบริการของศูนย์สหกิจศึกษาฯ |

> **2 ข้อนี้คือคำถามชุดเดียวกับที่อยู่ในฟอร์มสถานประกอบการ ([G5](#g5--ย้ายและแปลงส่วนประเมินกระบวนการศูนย์สหกิจศึกษาฯ)) และฟอร์มอาจารย์นิเทศ** (ถ้อยคำต่างกันเล็กน้อยเพราะคนละต้นฉบับ)
> → **ต้องใช้จาก `lib/coop-center-copy.ts` โมดูลเดียวกัน** ไม่ใช่เก็บถ้อยคำชุดที่สาม ถ้าถ้อยคำต่างกันจริงต้องเลือกชุดเดียวให้ทั้ง 3 ฟอร์มใช้ (ดู [Q14](#q14--ถ้อยคำ-2-ข้อของศูนย์สหกิจฯ-ต่างกัน-3-ฉบับ-จะยึดฉบับไหน))

**กลุ่มย่อย: การสนับสนุนจากสถานประกอบการ** (4 ระดับ, บังคับตอบ)

| # | คำถาม |
|---|---|
| N+5 | ความเอาใจใส่ดูแลนักศึกษาของพนักงานที่ปรึกษาหรือฝ่ายบุคคล |
| N+6 | การจัดให้มีพนักงานที่ปรึกษาที่มีความรู้และประสบการณ์ดูแลนักศึกษา |
| N+7 | การสนับสนุนด้านสวัสดิการและค่าตอบแทนที่เหมาะสม |
| N+8 | ความเหมาะสมของที่ตั้งสถานประกอบการและสภาพแวดล้อมที่ปลอดภัย |
| N+9 | ความพึงพอใจต่อสถานประกอบการโดยภาพรวม |

**กลุ่มย่อย: ข้อคิดเห็น**

| # | คำถาม | ชนิด |
|---|---|---|
| N+10 | ควรจัดส่งนักศึกษามาปฏิบัติงานสหกิจศึกษาที่หน่วยงานนี้ต่อไปหรือไม่ | ข้อความยาว |
| N+11 | ข้อคิดเห็นเพิ่มเติม | ข้อความยาว |

> **ข้อเสนอ:** ข้อ N+10 ในต้นฉบับเป็นช่องข้อความเปล่า ทั้งที่เป็นคำถามใช่/ไม่ใช่ชัดเจน และฝั่งอาจารย์นิเทศถามเรื่องเดียวกันด้วยตัวเลือก (`adv_future_placement`) → ควรเปลี่ยนเป็นตัวเลือก + ช่องเหตุผล จะได้เอาไปทำสถิติได้ (ดู [Q15](#q15--ข้อ-ควรจัดส่งนักศึกษามาที่หน่วยงานนี้ต่อไป-เปลี่ยนเป็นตัวเลือกไหม))

### การรันเลขข้อใหม่ (จุดที่พังง่ายที่สุด)

จำนวน LO ต่างกันทุกหลักสูตร → เลขข้อของส่วนที่ 5 และ 6 **ต้องคำนวณตอน render**

```
N = จำนวนคำถาม LO ทั้งหมดของหลักสูตรนั้น (ชุดแรก + ชุดสอง)
เลขข้อของส่วนที่ 5 เริ่มที่ N + 1
```

**ห้าม hard-code เลข 23–33 เด็ดขาด** — ตัวเลขในตารางข้างบนเขียนเป็น `N+k` ด้วยเหตุผลนี้
ต้องมีตัวนับเลขข้อที่เดินต่อเนื่องข้ามส่วน ไม่ใช่ให้แต่ละส่วนเริ่มนับ 1 ใหม่

### งานเชิงเทคนิค

**นี่คือฟอร์มใหม่ทั้งฟอร์ม ไม่ใช่การแก้ของเดิม — ประเมินว่าเป็นงานที่ใหญ่ที่สุดใน phase นี้**

- `web/src/lib/routes.ts` — `isFormRole` ต้องรับ `student` เพิ่ม
- `web/src/app/[school]/[program]/[role]/[lang]/page.tsx` — เพิ่มสาขา `role === "student"`
- `web/src/app/[school]/[program]/student/StudentWizard.tsx` — wizard ใหม่
- `web/src/app/[school]/[program]/student/actions.ts` — server action ใหม่ (โหลด/บันทึกร่าง + submit)
- `web/src/components/student/*` — step components + copy
- `web/src/lib/student-schema.ts` — Zod schema ใหม่
- DB — ต้องมีที่เก็บ submission ของฟอร์มนี้ (ดู [Q16](#q16--เก็บ-submission-ของฟอร์มนักศึกษาไว้ที่ไหน))
- `web/src/components/ui/form-picker-dialog.tsx` — เพิ่มตัวเลือกที่ 3 (ตอนนี้รองรับแค่ 2 ฟอร์ม)
- `CONTEXT.md` — เพิ่มคำศัพท์ "แบบสอบถามนักศึกษา" เข้า glossary

**นำกลับมาใช้ซ้ำได้ทั้งดุ้น** (อย่าเขียนใหม่):
- `RatingCard` + prop `disabledValues`
- ตรรกะ `PRIMARY_DOMAINS` / `dedupeByLoCode` / `questionText` / `optionLabel` / `sectionTitle`
- แพตเทิร์น draft autosave + `formVersion` sync
- `ErrorSummary`, `StepProgressBar`, `Stepper`, `fields.tsx`, `ChoiceGroup`
- `lib/coop-center-copy.ts`

### เกณฑ์ผ่าน

- [ ] เปิด `/{school}/{program}/student/th` ได้ และ `/en` ด้วย
- [ ] ส่วนที่ 3–4 แสดง LO ของหลักสูตรนั้นจริง ไม่ใช่คำถามตายตัว 22 ข้อ
- [ ] เปิดคนละหลักสูตร → ได้ชุด LO คนละชุด และ**เลขข้อของส่วนที่ 5–6 เลื่อนตามถูกต้อง**
- [ ] ไม่มีเลขข้อ hard-code ในโค้ด (`grep` หาเลข 23–33 ไม่ควรเจอในบริบทเลขข้อ)
- [ ] ทุกข้อคะแนนเป็น 4 ระดับ ไม่มี 5 ระดับหลงเหลือ
- [ ] เลือก "ไม่มี" ในข้อค่าตอบแทน → ซ่อนข้อย่อย และค่าไม่ค้างใน payload
- [ ] คำชี้แจง "ไม่มีผลต่อการประเมินผลปฏิบัติงาน" แสดงเด่นที่หัวฟอร์ม
- [ ] กดข้ามส่วนได้อิสระ ไม่มี validation บล็อก
- [ ] บันทึกร่างแล้วรีเฟรช → ค่ากลับมาครบทุกส่วน
- [ ] หน้าเลือกฟอร์มแสดง 3 ตัวเลือก
- [ ] โหมด th ไม่มีอักษรละติน โหมด en ไม่มีอักษรไทย (ต้องแปลทั้งฟอร์มเป็นอังกฤษ — **ต้นฉบับ PDF มีแต่ภาษาไทย** ดู [Q17](#q17--ฟอร์มนักศึกษาต้องมีภาษาอังกฤษไหม))

---

## G9 — จัดโครงสร้างฝั่งอาจารย์นิเทศใหม่

> ✅ **ยืนยันแล้วว่าเสร็จ** (ทำไว้ก่อนหน้าใน commit af59a57 โดย agent ชุดก่อน) — ผมตรวจสอบซ้ำด้วยการอ่านโค้ดตรง ๆ ทุกข้อ: ADVISOR_STEPS เรียงถูกต้อง, ไม่มี "LO ชุดแรก/ชุดสอง" หรือ "ด้านที่ 6..." หลงเหลือ, adv_premium_workplace + superRefine ครบ, workplaceScore ไม่รวมคะแนนพรีเมี่ยม (แยก field prefix ชัดเจน).
> **ยืนยันขอบเขต 2026-09-11:** ฝั่งอาจารย์นิเทศถูกจัดลำดับส่วนใหม่ทั้งฟอร์ม ไม่ใช่แค่สเกลรายงาน

### โครงสร้างใหม่ (ยืนยันแล้ว)

| ส่วน | ชื่อใหม่ | เดิมอยู่ที่ | หมายเหตุ |
|---|---|---|---|
| 1 | ข้อมูลทั่วไป | ส่วนที่ 1 | ไม่เปลี่ยน |
| 2 | **ความรู้และทักษะ** | ส่วนที่ 2 "LO ชุดแรก" | **เปลี่ยนแค่ชื่อ** ให้ตรงฝั่งสถานประกอบการ |
| 3 | **จริยธรรมและบุคลิก** | ส่วนที่ 3 "LO ชุดสอง" | **เปลี่ยนแค่ชื่อ** ให้ตรงฝั่งสถานประกอบการ |
| 4 | **รายงาน/โครงงาน** | ส่วนที่ 6 | **ย้ายขึ้นมา** |
| 5 | **ข้อคิดเห็น** | ส่วนที่ 4 "อื่น ๆ" | **เปลี่ยนชื่อ + เลื่อนลง** |
| 6 | ศูนย์สหกิจฯ + สถานประกอบการ | ส่วนที่ 5 | **เลื่อนลง** + เพิ่มข้อ 6 |

### G9.1 — เปลี่ยนชื่อส่วนที่ 2 และ 3 ให้ตรงกับฝั่งสถานประกอบการ

`web/src/components/advisor/advisor-steps-core.tsx` → `ADVISOR_STEPS`

| ตำแหน่ง | ไทย เดิม | ไทย ใหม่ | อังกฤษ เดิม | อังกฤษ ใหม่ |
|---|---|---|---|---|
| index 1 | LO ชุดแรก | **ความรู้และทักษะ** | LO set 1 | **Knowledge and skills** |
| index 2 | LO ชุดสอง | **จริยธรรมและบุคลิก** | LO set 2 | **Ethics and character** |

> คำว่า "LO ชุดแรก / ชุดสอง" เป็นศัพท์ภายในที่คนนอกไม่เข้าใจ — ใช้ชื่อเดียวกับฝั่งสถานประกอบการ (`WIZARD_COPY[locale].steps[1][0]` / `[2][0]`) เพื่อให้ทั้งสองฟอร์มพูดภาษาเดียวกัน **ควร import ชื่อจากที่เดียวกัน ไม่ใช่พิมพ์ซ้ำ**

### G9.2 — เปลี่ยนชื่อส่วนที่ 5 จาก "อื่น ๆ" เป็น "ข้อคิดเห็น"

- `ADVISOR_STEPS` index ของส่วนนี้: ชื่อสั้น `อื่น ๆ` → **`ข้อคิดเห็น`** / `Other` → **`Comments`**
- `advisor-copy.ts` → `otherTitle: "ด้านอื่น ๆ"` → **`"ข้อคิดเห็น"`** / `"Others"` → **`"Comments"`**

### G9.3 — แก้ข้อความ 2 ข้อในส่วนข้อคิดเห็น (ตัดคำนำหน้าออก)

`advisor-copy.ts` → `otherItems` ปัจจุบันมีคำนำหน้า `"ด้านที่ 6 ด้านอื่น ๆ: "` ติดมาจากแบบฟอร์มกระดาษ — **ตัดออกทั้งสองข้อ**

| # | ไทย (ใหม่) | บังคับตอบ |
|---|---|---|
| 1 | นักศึกษาสหกิจศึกษาสามารถใช้ภาษาอังกฤษหรือภาษาอื่น ๆ ที่เกี่ยวข้องกับการทำงานและในชีวิตประจำวัน | ✔ |
| 2 | โดยภาพรวม ท่านคิดเห็นว่านักศึกษาสหกิจศึกษามีผลการปฏิบัติงานระดับใด | ✔ |

ฝั่งอังกฤษตัดคำนำหน้า `"Topic 6 Others: "` ออกเช่นกัน:
1. `Ability to use English or other language skills in the working environment and daily life`
2. `Overall Performance Rating`

### G9.4 — เพิ่มข้อ 6 "หน่วยงานพรีเมี่ยม" ในส่วนที่ 6

ต่อท้ายรายการ `workplaceItems` ถัดจากข้อ 5 *"ความพึงพอใจต่อสถานประกอบการโดยภาพรวม"*

**คำถาม:** `6. ควรเป็นหน่วยงานพรีเมี่ยมหรือไม่` **(บังคับตอบ)**

**ไม่ใช่สเกล 4 ระดับ** — เป็นตัวเลือก 3 ทาง:

| ค่า | ไทย | อังกฤษ (เสนอ — รอยืนยัน [Q12](#q12--คำแปลอังกฤษของข้อ-6-หน่วยงานพรีเมี่ยม)) |
|---|---|---|
| `yes` | ใช่ | Yes |
| `no` | ไม่ใช่ | No |
| `review` | ทบทวนดูก่อน | Needs further review |

**เมื่อเลือก "ทบทวนดูก่อน" → ต้องระบุเหตุผล** (ช่องข้อความบังคับกรอก เด้งออกมาเมื่อเลือกข้อนี้)

> **ใช้แพตเทิร์นเดิมที่มีอยู่แล้ว** — เหมือน `adv_future_placement` + `adv_future_placement_other` ใน `advisor-schema.ts` ที่ใช้ `superRefine` บังคับกรอกช่อง "โปรดระบุ" เมื่อเลือก `other` (ดู `advisor-steps-extra.tsx:243`) ลอกโครงนั้นมาได้เลย ไม่ต้องคิดใหม่

**ฟิลด์ใหม่:** `adv_premium_workplace` (enum) + `adv_premium_workplace_reason` (text, บังคับเมื่อ = `review`)

**ไม่รวมในคะแนน** — เป็นคำถามเชิงนโยบายของศูนย์สหกิจฯ ไม่ใช่คะแนนนักศึกษา ต้องไม่เข้า `workplaceScore`

### G9.5 — แก้เลข "ส่วนที่ N" ที่ฝังอยู่ในหัวข้อกลุ่ม

**นี่คือจุดที่พังง่ายที่สุดของ G9** — `advisor-copy.ts` ฝังเลขส่วนไว้ในข้อความหัวข้อโดยตรง พอสลับลำดับแล้วเลขจะผิดทันที **ยืนยันแล้วว่าให้แก้เลขให้ตรงลำดับใหม่**

| ตัวแปร | เดิม | ใหม่ |
|---|---|---|
| `reportTitle` | ส่วนที่ **4** แบบประเมินผลรายงานสหกิจศึกษา | ส่วนที่ **4** แบบประเมินผลรายงานสหกิจศึกษา ✅ บังเอิญตรงอยู่แล้ว |
| `centerTitle` | ส่วนที่ **2** ประเมินกระบวนการดำเนินงานของศูนย์สหกิจศึกษาและพัฒนาอาชีพ | ส่วนที่ **6** … |
| `workplaceTitle` | ส่วนที่ **3** ความเข้าใจเรื่องสหกิจศึกษาและการสนับสนุนจากสถานประกอบการ | ส่วนที่ **6** … |
| `otherTitle` | ด้านอื่น ๆ (ไม่มีเลข) | ส่วนที่ **5** ข้อคิดเห็น |

⚠️ **ปัญหาที่ต้องตัดสินใจ:** ส่วนที่ 6 มี **2 หัวข้อกลุ่ม** อยู่ในหน้าเดียว (ศูนย์สหกิจฯ + สถานประกอบการ) จะได้ "ส่วนที่ 6" ซ้ำกัน 2 อัน ดูแปลก
→ เสนอ: ให้ส่วนที่ 6 มีหัวข้อใหญ่อันเดียว แล้วสองกลุ่มย่อยเป็นหัวข้อรองที่ไม่มีเลข (`6.1` / `6.2` หรือไม่มีเลขเลย) — ดู [Q13](#q13--ส่วนที่-6-มี-2-หัวข้อกลุ่ม-จะใส่เลขยังไง)

**และฝั่งอังกฤษต้องแก้คู่กันทุกบรรทัด** (`Part 2:` → `Part 6:` ฯลฯ) ไม่งั้นผิดกติกา "แยกภาษาเด็ดขาด" ไปคนละทาง

> **ข้อเสนอเชิงโครงสร้าง:** เลิกฝังเลขส่วนไว้ในสตริง แล้วให้คอมโพเนนต์ประกอบเลขจาก `ADVISOR_STEPS` ตอน render แทน — ครั้งนี้ก็เกิดปัญหานี้แล้ว และ phase หน้าที่สลับลำดับอีกจะเกิดซ้ำแน่นอน

### งานเชิงเทคนิค

- `web/src/components/advisor/advisor-steps-core.tsx` — `ADVISOR_STEPS` เรียงใหม่ทั้ง `th` และ `en`
- `web/src/components/advisor/advisor-copy.ts` — ชื่อหัวข้อ, `otherItems`, `workplaceItems` (เพิ่มข้อ 6), เลขส่วน, ลบ `rating5` ([G4](#g4--รายงานโครงงาน-ลดจาก-5-ระดับเหลือ-4-ระดับ))
- `web/src/components/advisor/advisor-steps-extra.tsx` — เพิ่มช่องคำถามพรีเมี่ยม + ช่องเหตุผลแบบ disclosure
- `web/src/lib/advisor-schema.ts` — เพิ่ม `adv_premium_workplace` + `superRefine` บังคับเหตุผล; ย้าย field ระหว่าง step schema ให้ตรงลำดับใหม่
- `web/src/app/[school]/[program]/advisor/AdvisorWizard.tsx` — **สลับ index ของบล็อก `hidden={currentStep !== N}` ทั้ง 6 บล็อก** (บรรทัด ~436–465) + `validateStep` + `hasMinimumData` + `fieldStep`
- `web/src/app/[school]/[program]/advisor/actions.ts` — ลำดับการ validate และคีย์คะแนน

> ⚠️ **ทำ [G6](#g6--โครงสร้างใหม่-6-ส่วน-ฝั่งสถานประกอบการ) เฟสเตรียม (เปลี่ยนเลข step เป็นค่าคงที่แบบมีชื่อ) ให้ครอบคลุม `AdvisorWizard` ด้วย** — ฟอร์มนี้สลับลำดับ 4 ส่วนพร้อมกัน ถ้ายังใช้ตัวเลขดิบจะพลาดแน่

### เกณฑ์ผ่าน

- [ ] แถบส่วนแสดง 6 ส่วนตามลำดับใหม่ ทั้ง `th` และ `en`
- [ ] ไม่มีคำว่า "LO ชุดแรก / ชุดสอง" และ "ด้านที่ 6 ด้านอื่น ๆ:" หลงเหลือ
- [ ] เลข "ส่วนที่ N" ในหัวข้อกลุ่มทุกอัน ตรงกับตำแหน่งจริงบนแถบส่วน
- [ ] ข้อ 6 พรีเมี่ยม: ไม่ตอบ → กดส่งไม่ผ่าน
- [ ] เลือก "ทบทวนดูก่อน" → เด้งช่องเหตุผล และเว้นว่างแล้วกดส่งไม่ผ่าน
- [ ] เลือก "ใช่"/"ไม่ใช่" แล้วสลับกลับ → ค่าเหตุผลเดิมไม่ค้างไปกับ payload
- [ ] คะแนนพรีเมี่ยมไม่โผล่ในคะแนนสถานประกอบการที่หน้าสรุปผล
- [ ] ร่างเก่าที่บันทึกไว้ด้วยลำดับเดิม เปิดแล้วไม่เด้งไปผิดส่วน (ต้องมี guard — `draft.currentStep` ที่เก็บไว้ชี้คนละส่วนแล้ว)

---

## G8 — คู่มือการใช้งานระบบ (สำหรับส่งให้หน่วยงานตรวจ)

### เป้าหมาย

คู่มือที่ **ส่งให้สถานประกอบการอ่านแล้วกรอกฟอร์มได้เองจนจบ โดยไม่ต้องโทรถาม**

**กลุ่มผู้อ่านหลัก:** พี่เลี้ยง/หัวหน้างานในสถานประกอบการ ซึ่งส่วนใหญ่ **ไม่ถนัดเทคโนโลยี** — สมมติฐานในการเขียนคือผู้อ่านไม่รู้ว่า "ปุ่มถัดไปอยู่ตรงไหน", "ทำไมกดแล้วไม่ไป", "กรอกค้างไว้แล้วจะกลับมาทำต่อยังไง"

**หลักการเขียน:**
- ทุกขั้นตอนต้องมี **ภาพหน้าจอจริง** พร้อมกรอบ/ลูกศรชี้จุดที่ต้องกด — ห้ามอธิบายด้วยตัวหนังสือล้วน
- เขียนเป็นคำสั่งทีละขั้น ("กดปุ่ม **ถัดไป** ที่มุมขวาล่าง") ไม่ใช่คำบรรยายระบบ
- **ห้ามใช้ศัพท์เทคนิค** — ไม่มีคำว่า browser, cache, localStorage, validation, draft token ในตัวคู่มือ (อธิบายด้วยคำชาวบ้านแทน)
- ทุกหัวข้อตอบคำถาม "แล้วฉันต้องกดตรงไหน" ให้ได้ภายใน 1 ภาพ

### โครงเรื่องที่ต้องมี

| # | หัวข้อ | เนื้อหาสำคัญ |
|---|---|---|
| 1 | แบบประเมินนี้คืออะไร ใครเป็นคนกรอก | แยกให้ชัดว่าเล่มนี้สำหรับ **สถานประกอบการ** ไม่ใช่อาจารย์นิเทศ + ใช้เวลาประมาณกี่นาที |
| 2 | เตรียมข้อมูลให้พร้อมก่อนเริ่ม | เช็กลิสต์: อีเมล, ชื่อสถานประกอบการ, ชื่อ–ตำแหน่ง–แผนกผู้ประเมิน, เบอร์โทร, **รหัสนักศึกษา 8 หลัก**, ชื่อ–สกุลนักศึกษา, ปีการศึกษา/ภาคการศึกษา |
| 3 | เข้าระบบยังไง | 2 ทาง: (ก) กดลิงก์ที่ศูนย์สหกิจฯ ส่งให้ — **ทางที่แนะนำ** (ข) เข้าหน้าแรก → เลือกสำนักวิชา → เลือกหลักสูตร → เลือกฟอร์ม |
| 4 | ทำความรู้จักหน้าจอ | ภาพเดียวจบ ชี้ให้เห็น: แถบ 6 ส่วนด้านบน, ปุ่มถัดไป/ย้อนกลับ, ข้อความบอกสถานะบันทึก, ปุ่มสลับภาษา ไทย/English |
| 5 | กรอกทีละส่วน (6 ส่วน) | ภาพหน้าจอ + คำอธิบายทุกส่วนตาม [G6](#g6--โครงสร้างใหม่-6-ส่วน-ฝั่งสถานประกอบการ) |
| 6 | **กรอกค้างไว้แล้วกลับมาทำต่อ** | ⚠️ หัวข้อสำคัญที่สุด — ดูข้อควรระวังด้านล่าง |
| 7 | **จะแก้คำตอบต้องทำยังไง** | แยก 2 กรณีให้ชัด: ก่อนกดส่ง / หลังกดส่งแล้ว |
| 8 | กดส่งแบบประเมิน | ภาพปุ่มส่ง + หน้าขอบคุณ + **รหัสอ้างอิง ให้บันทึก/ถ่ายภาพเก็บไว้** |
| 9 | ปัญหาที่พบบ่อย | ดูตารางด้านล่าง |
| 10 | ติดต่อใคร | ชื่อ–เบอร์–อีเมลเจ้าหน้าที่ศูนย์สหกิจฯ + เวลาทำการ |

### ⚠️ 4 เรื่องที่คู่มือ "ต้อง" เตือน ไม่งั้นหน่วยงานโทรเข้าแน่

ทั้ง 4 ข้อนี้มาจากพฤติกรรมจริงของระบบที่ตรวจจากโค้ดแล้ว — เป็นจุดที่ผู้ใช้ low-tech พลาดง่ายที่สุด

**1. ร่างที่กรอกค้างไว้ ผูกกับ "เครื่องเดิม + เบราว์เซอร์เดิม" เท่านั้น**
ระบบบันทึกร่างอัตโนมัติ แต่จำร่างไว้ที่เครื่องผู้ใช้ (`localStorage` key `evaluation-draft:{programId}:{templateId}` ใน `EvaluationWizard.tsx`) ผลคือ:
- กรอกค้างที่คอมพิวเตอร์ที่ทำงาน แล้วไปเปิดต่อที่มือถือ → **ร่างไม่มา ต้องกรอกใหม่หมด**
- เปิดด้วยโหมดไม่ระบุตัวตน (Incognito) หรือล้างประวัติการใช้งาน → **ร่างหาย**
- ใช้คนละเบราว์เซอร์ (Chrome ↔ Edge) บนเครื่องเดียวกัน → **ร่างไม่มา**

คู่มือต้องบอกตรง ๆ ว่า *"ให้กรอกจนจบด้วยเครื่องเดิมและโปรแกรมเดิมที่เริ่มไว้"* และแนะนำว่าถ้ากรอกไม่จบในครั้งเดียว ให้เปิดหน้าเดิมค้างไว้

**2. ส่งแล้วแก้ไม่ได้**
เมื่อกดส่งสำเร็จ ระบบล้างร่างทิ้งและ**ไม่มีหน้าสำหรับกลับไปแก้คำตอบที่ส่งแล้ว** คู่มือต้อง:
- เตือนก่อนถึงปุ่มส่งว่า *"กรุณาตรวจทานให้ครบทุกส่วนก่อนกดส่ง เพราะส่งแล้วจะแก้ไขเองไม่ได้"*
- บอกทางออกให้ชัด: ถ้าส่งผิดต้องติดต่อเจ้าหน้าที่ศูนย์สหกิจฯ พร้อมแจ้ง **รหัสอ้างอิง** ที่ได้จากหน้าขอบคุณ
- ⚠️ *ถ้าศูนย์สหกิจฯ อยากให้แก้เองได้ นี่คือฟีเจอร์ใหม่ที่ยังไม่มีในระบบ ต้องตั้งเป็นงาน phase ถัดไป* (ดู [Q10](#q10--ต้องให้แก้ไขคำตอบหลังส่งได้ไหม))

**3. ปุ่ม "แก้ไข" บนแถบเครื่องมือ ไม่ใช่ปุ่มแก้คำตอบ**
แถบเครื่องมือเหนือฟอร์มมีปุ่ม `ดู | แก้ไข | พิมพ์ | ดาวน์โหลดเอกสาร | ประวัติการแก้ไข` ปุ่ม **แก้ไข** พาไปหน้าแก้ **ชุดคำถามของแบบประเมิน** (สำหรับเจ้าหน้าที่) ไม่ใช่แก้คำตอบที่ตัวเองกรอก

**และหน้านั้นไม่มีระบบล็อกอินป้องกัน** — ผู้ใช้ที่กดเข้าไปแล้วลองแก้ สามารถเปลี่ยนคำถามของหลักสูตรได้จริง

คู่มือต้องเขียนเตือนไว้ชัด ๆ ว่าปุ่มชุดนี้ *"สำหรับเจ้าหน้าที่ ไม่ต้องกด"* แต่ **การเตือนในคู่มือไม่ใช่การแก้ปัญหา** — ควรซ่อนปุ่ม แก้ไข/ประวัติ ออกจากหน้าฟอร์มที่ส่งให้บุคคลภายนอก หรือใส่การยืนยันตัวตนก่อน (ดู [Q11](#q11--ซ่อนปุ่มแก้ไขประวัติจากผู้ใช้ภายนอกไหม)) ผมแนะนำให้ทำจริงใน phase นี้เลย เพราะกำลังจะส่งลิงก์ให้คนนอกใช้

**4. กดข้ามส่วนได้ และจะไม่มี error จนกว่าจะกดส่ง**
เป็นพฤติกรรมที่ **ตั้งใจออกแบบ** (กติกาโปรเจกต์ "ห้าม gate navigation") ไม่ใช่ระบบพัง คู่มือต้องอธิบายว่า:
- กดแถบส่วนด้านบนเพื่อกระโดดไปส่วนไหนก็ได้ ไม่ต้องกรอกเรียงลำดับ
- **เครื่องหมายถูก ✓ บนแถบส่วน = ส่วนนั้นตอบครบแล้ว** ให้ใช้เป็นตัวเช็กก่อนกดส่ง
- ถ้ากดส่งแล้วยังกรอกไม่ครบ ระบบจะขึ้นกล่องสรุปข้อผิดพลาดด้านบน **กดที่รายการในกล่องแล้วระบบจะพาไปยังช่องที่ต้องแก้ให้เอง**

### ตารางปัญหาที่พบบ่อย (ต้องมีในคู่มือ)

| อาการ | สาเหตุ | วิธีแก้ |
|---|---|---|
| เลือกภาคการศึกษาไม่ได้ | ยังไม่ได้เลือกปีการศึกษา | เลือกปีการศึกษาก่อน แล้วช่องภาคการศึกษาจะเปิดให้เลือก (2569 มี 3 ภาค / 2570 มี 2 ภาค) |
| กรอกรหัสนักศึกษาแล้วชื่อไม่ขึ้นเอง | ระบบยังไม่มีฐานข้อมูลค้นชื่ออัตโนมัติ | พิมพ์ชื่อ–สกุลนักศึกษาเองในช่องถัดไป |
| กดส่งแล้วไม่ไปไหน | ยังกรอกไม่ครบ | เลื่อนขึ้นดูกล่องสรุปข้อผิดพลาดสีแดงด้านบน แล้วกดรายการในกล่อง |
| เปิดมาแล้วคำตอบที่กรอกไว้หายหมด | เปิดคนละเครื่อง/คนละเบราว์เซอร์/ล้างประวัติ | ต้องกรอกใหม่ — ครั้งนี้ให้ใช้เครื่องเดิมจนจบ |
| การ์ดคะแนนบางข้อมีคำอธิบายยาว บางข้อมีแค่คำสั้น ๆ | หลักสูตรที่กำหนดเกณฑ์ละเอียดไว้จะแสดงคำอธิบาย ([G3](#g3--rubric-ของ-los-ตัดป้ายระดับออก-เหลือเลข--คำอธิบาย)) | ปกติ ไม่ใช่ข้อผิดพลาด |
| ในส่วนที่ 2 กดระดับ 1 ไม่ได้ | ตั้งใจล็อกไว้ — ติ๊กทักษะมาแล้วแปลว่าทักษะนั้นเกี่ยวข้อง จึงเลือก "ไม่จำเป็น" ไม่ได้ ([G2](#g2--ส่วนที่-2-ใหม่-ทักษะที่สถานประกอบการคาดหวังจากนักศึกษา)) | เลือกระดับ 2–5 หรือถ้าไม่ต้องการข้อนั้นให้ติ๊กออก |
| หน้าเป็นภาษาอังกฤษ อยากได้ภาษาไทย | ระบบเปิดภาษาอังกฤษอัตโนมัติสำหรับหลักสูตรนานาชาติ | กดปุ่มสลับภาษาที่มุมขวาบน |

### งานที่ต้องทำ

- [ ] เขียนโครงเรื่อง + ข้อความคู่มือ (ยังไม่ต้องมีภาพ) — ทำคู่ขนานกับงาน G1–G6 ได้
- [ ] เก็บภาพหน้าจอทุกขั้นตอน **หลัง UI นิ่งแล้ว** (หลัง G6 เสร็จ) — ใช้ข้อมูลตัวอย่างที่ไม่ใช่ข้อมูลจริงของนักศึกษา
- [ ] ตีกรอบ/ใส่ลูกศรชี้จุดที่ต้องกดในทุกภาพ
- [ ] ใส่ข้อมูลติดต่อเจ้าหน้าที่ศูนย์สหกิจฯ จริง
- [ ] ให้เจ้าหน้าที่ศูนย์สหกิจฯ ทดลองทำตามคู่มือแบบไม่มีคนช่วย 1 รอบ แล้วจดจุดที่สะดุด
- [ ] ส่งให้หน่วยงานตรวจ

**ไฟล์ปลายทางที่แนะนำ:** `docs/manual/คู่มือการใช้งาน-สถานประกอบการ.md` + ภาพใน `docs/manual/images/` แล้ว export เป็น PDF สำหรับส่ง

> **หมายเหตุลำดับงาน:** ถ้าทำคู่มือก่อน G1–G6 เสร็จ ภาพหน้าจอจะใช้ไม่ได้ทั้งหมด เพราะโครงสร้างส่วนเปลี่ยนจาก 6 ส่วนเดิมเป็น 6 ส่วนใหม่ (คนละชุด) และสเกลรายงานเปลี่ยนจาก 5 เป็น 4 การ์ด → **เขียนข้อความก่อนได้ แต่ถ่ายภาพหลังสุด**

### เกณฑ์ผ่าน

- [ ] คนที่ไม่เคยเห็นระบบมาก่อน อ่านคู่มืออย่างเดียวแล้วกรอกจนกดส่งได้สำเร็จ โดยไม่ถามใคร
- [ ] ทุกขั้นตอนที่ต้องกด มีภาพประกอบชี้จุด
- [ ] มีคำเตือนครบทั้ง 4 เรื่องข้างต้น
- [ ] ไม่มีศัพท์เทคนิคหลงเหลือ
- [ ] มีข้อมูลติดต่อเจ้าหน้าที่จริง ไม่ใช่ placeholder

---

## คำถามค้าง — รอคำตอบก่อนลงมือบางส่วน

### Q1 — คำแปลภาษาอังกฤษของชื่อฟอร์ม
`CONTEXT.md` ล็อกกติกา "แยกภาษาเด็ดขาด" ไว้ จึงต้องมีคำแปลอังกฤษของชื่อเต็มทั้งสอง ผมเสนอ `Cooperative Education Performance Evaluation by the Workplace` และ `Cooperative Education Student Evaluation by the Faculty Advisor` — ใช้ได้ไหม หรือมีชื่ออังกฤษทางการจากศูนย์สหกิจฯ อยู่แล้ว?

### ~~Q2 — รายงานฝั่งอาจารย์นิเทศเปลี่ยนเป็น 4 ระดับด้วยไหม~~ ✅ ปิดแล้ว 2026-09-11
**ตอบ: เปลี่ยนด้วย** — "ทั้งระบบ" หมายรวมฝั่งอาจารย์นิเทศ แต่แตะแค่สเกลรายงานเรื่องเดียว โครงสร้างส่วนอื่นของฟอร์มอาจารย์นิเทศยังไม่แตะ → ย้ายไปเป็นขอบเขตของ [G4](#g4--รายงานโครงงาน-ลดจาก-5-ระดับเหลือ-4-ระดับ) แล้ว

### Q3 — รูปแบบการเก็บข้อมูลส่วนที่ 2
ระดับความจำเป็นของ 16 ทักษะ จะเอาไปทำรายงานสถิติแบบไหน?
- (ก) เก็บใน `payload_json` พอ — ค้นทีหลังด้วย SQL ได้แต่ช้าและไม่มี index
- (ข) เพิ่มตาราง `skill_expectations` (submission_id, skill_no, level) — ทำ dashboard ง่าย แต่งานเพิ่ม
*(ถ้าตั้งใจจะทำหน้าสรุปว่า "สถานประกอบการคาดหวังทักษะอะไรมากที่สุด" แนะนำ (ข) ตั้งแต่แรก)*

### Q4 — ป้ายระดับ 4/3/2/1 ที่ไม่ตรงกันระหว่างสองชุด
รายงานใช้ `ดีมาก/ดี/พอใช้/ต้องปรับปรุง` แต่กระบวนการศูนย์สหกิจฯ ใช้ `ดีเยี่ยม/ดี/พอใช้/ควรปรับปรุง` — ตั้งใจให้ต่างกันไหม หรือให้ผมรวมเป็นชุดเดียว?

### Q5 — คำสะกด "แรวคิดแบบเติบโต"
ทักษะข้อ 16 ในต้นฉบับสะกดว่า "แรวคิดแบบเติบโต" ผมจะแก้เป็น "แนวคิดแบบเติบโต" — ถ้าเอกสารทางการสะกดแบบเดิมจริง แจ้งกลับด้วย

### Q6 — มี submission จริงในระบบแล้วกี่รายการ
ผลต่อขนาดงาน [G4](#g4--รายงานโครงงาน-ลดจาก-5-ระดับเหลือ-4-ระดับ) โดยตรง — ถ้ายังเป็น 0 ข้าม migration การแปลงคะแนนได้เลย ประหยัดไปครึ่งงาน

### Q7 — คู่มือทำกี่เล่ม
ฝั่งสถานประกอบการกับฝั่งอาจารย์นิเทศเป็นคนละฟอร์มคนละผู้อ่าน — แยก 2 เล่ม (ผมแนะนำ เพราะผู้อ่านคนละกลุ่ม ส่งคนละที่) หรือเล่มเดียวแบ่ง 2 ภาค?

### Q8 — คู่มือต้องมีฉบับภาษาอังกฤษไหม
ระบบรองรับโหมด `en` และหลักสูตรนานาชาติเปิดภาษาอังกฤษอัตโนมัติ — ถ้ามีสถานประกอบการต่างชาติ ต้องมีคู่มือ `en` ด้วย

### Q9 — รูปแบบไฟล์คู่มือที่จะส่ง
PDF อย่างเดียว / PDF + หน้าช่วยเหลือในระบบ (`/help`) ที่ผู้ใช้กดดูได้ระหว่างกรอก?
*(ผมแนะนำมีหน้าในระบบด้วย เพราะผู้ใช้ low-tech มักหาไฟล์ PDF ที่ดาวน์โหลดไว้ไม่เจอ)*

### Q10 — ต้องให้แก้ไขคำตอบหลังส่งได้ไหม
ตอนนี้ส่งแล้วแก้เองไม่ได้เลย ถ้าศูนย์สหกิจฯ ต้องการให้แก้ได้ ต้องทำระบบเปิดสิทธิ์แก้ไข (ลิงก์แก้ไขผูกรหัสอ้างอิง / ให้เจ้าหน้าที่ปลดล็อกให้) — เป็นงานใหม่ ไม่อยู่ใน phase นี้ แต่ต้องรู้ก่อนเขียนคู่มือว่าจะเขียนทางออกยังไง

### Q11 — ซ่อนปุ่มแก้ไข/ประวัติ จากผู้ใช้ภายนอกไหม
หน้าแก้ชุดคำถาม (`/programs/{id}/edit`) **ไม่มีระบบล็อกอินป้องกัน** และปุ่มเข้าอยู่บนหน้าฟอร์มที่กำลังจะส่งลิงก์ให้คนนอก
ผมแนะนำให้ทำใน phase นี้เลย อย่างน้อยซ่อนปุ่มออกจากหน้าฟอร์ม (ยังเข้าผ่าน URL ตรงได้สำหรับเจ้าหน้าที่) — เห็นด้วยไหม หรือจะทำระบบยืนยันตัวตนเต็มรูปแบบ?

### Q12 — คำแปลอังกฤษของข้อ 6 หน่วยงานพรีเมี่ยม
ผมเสนอ `6. Should this be designated a premium workplace?` พร้อมตัวเลือก `Yes / No / Needs further review` และช่องเหตุผล `Please specify` — ใช้ได้ไหม หรือศูนย์สหกิจฯ มีศัพท์อังกฤษของ "หน่วยงานพรีเมี่ยม" อยู่แล้ว?

### Q14 — ถ้อยคำ 2 ข้อของศูนย์สหกิจฯ ต่างกัน 3 ฉบับ จะยึดฉบับไหน
คำถามประเมินศูนย์สหกิจฯ 2 ข้อ จะไปปรากฏใน **3 ฟอร์ม** แต่ถ้อยคำในต้นฉบับต่างกันเล็กน้อย
เช่น ฉบับอาจารย์นิเทศ/สถานประกอบการ ขึ้นต้นว่า "การดำเนินงานของศูนย์สหกิจศึกษาฯ เช่น การประสานงาน…" แต่ฉบับนักศึกษาว่า "การดำเนินการของศูนย์สหกิจศึกษา การประสานงาน…"
ผมแนะนำ **ยึดฉบับเดียวให้ทั้ง 3 ฟอร์ม** (ฉบับอาจารย์นิเทศที่ล็อกไว้แล้วเมื่อ 2026-09-07 พร้อมคำแปลอังกฤษ) — ตกลงไหม หรือแต่ละฟอร์มต้องใช้ถ้อยคำของตัวเอง?

### Q15 — ข้อ "ควรจัดส่งนักศึกษามาที่หน่วยงานนี้ต่อไป" เปลี่ยนเป็นตัวเลือกไหม
ในฟอร์มนักศึกษา ต้นฉบับเป็นช่องข้อความเปล่า ทั้งที่เป็นคำถามใช่/ไม่ใช่ และฝั่งอาจารย์นิเทศถามเรื่องเดียวกันด้วยตัวเลือก
เปลี่ยนเป็น `ควรส่ง / ไม่ควรส่ง / อื่น ๆ` + ช่องเหตุผล (เหมือน `adv_future_placement`) ไหม? จะได้เอาไปทำสถิติได้

### Q16 — เก็บ submission ของฟอร์มนักศึกษาไว้ที่ไหน
ตารางเดิม `evaluation_submissions` ออกแบบมาเพื่อคะแนน LO + รายงาน ไม่มีที่เก็บข้อมูลค่าใช้จ่าย/ที่พัก
- (ก) ใช้ตารางเดิม + คอลัมน์ `evaluator_role` แล้วยัดทุกอย่างลง `payload_json`
- (ข) ตารางใหม่ `student_survey_submissions` แยกต่างหาก
*(ผมแนะนำ (ก) ถ้าจะทำ [G7](#g7--คะแนนถ่วงน้ำหนัก-los-35--รายงาน-25--อาจารย์นิเทศ-40-บันทึกไว้-ยังไม่ทำ) ในอนาคต เพราะจับคู่ 3 ฟอร์มของนักศึกษาคนเดียวกันได้ง่ายกว่า)*

### ~~Q17 — ฟอร์มนักศึกษาต้องมีภาษาอังกฤษไหม~~ ✅ ปิดแล้ว 2026-09-11
**ตอบ: ต้องมี `en` ด้วย** — ฟอร์มนักศึกษาต้องรองรับ 2 ภาษาเต็มรูปแบบเหมือนอีก 2 ฟอร์ม
ไม่มีทางลัด redirect `/en` → `/th` แล้ว ต้องแปลทุกข้อความ (~40 รายการ: ชื่อส่วน, คำถามข้อมูลทั่วไป,
คำถามค่าใช้จ่าย/ที่พัก, ป้ายสวัสดิการ, จุดเด่น/สิ่งที่ต้องพัฒนา, ข้อคิดเห็น)

**ข้อความที่มีคำแปลอังกฤษอยู่แล้ว ให้ดึงมาใช้ ห้ามแปลใหม่:**
- คำถามศูนย์สหกิจฯ 2 ข้อ → `lib/coop-center-copy.ts`
- ชื่อส่วน "ความรู้และทักษะ" / "จริยธรรมและบุคลิก" → `WIZARD_COPY[en].steps`
- คำถาม LO ทั้งหมด → `text_en` / `label_en` / `description_en` จาก DB
- ป้ายสเกล 4 ระดับ → `ADVISOR_COPY[en].rating4`
- ฟิลด์ร่วม (รหัสนักศึกษา, ชื่อ–สกุล, สำนักวิชา, หลักสูตร, ภาคการศึกษา) → `WIZARD_COPY[en]`

เหลือที่ต้องแปลใหม่จริง ๆ คือกลุ่มค่าใช้จ่าย/ที่พัก (ส่วนที่ 2) และกลุ่มการสนับสนุนจากสถานประกอบการ
→ ให้ agent ร่างคำแปลมาก่อน แล้ว **ตั้งเป็นรายการให้ศูนย์สหกิจฯ ตรวจก่อนใช้จริง** ไม่ปล่อยขึ้นระบบทันที

### Q13 — ส่วนที่ 6 มี 2 หัวข้อกลุ่ม จะใส่เลขยังไง
ส่วนที่ 6 รวมทั้ง "ศูนย์สหกิจฯ" และ "สถานประกอบการ" ไว้หน้าเดียว ถ้าใส่ "ส่วนที่ 6" ทั้งคู่จะซ้ำกันดูแปลก
ผมเสนอ: หัวข้อใหญ่ "ส่วนที่ 6" อันเดียว แล้วสองกลุ่มย่อยไม่ใส่เลข — หรือจะแยกเป็น 2 ส่วน (กลายเป็น 7 ส่วน)?

---

## ลำดับการลงมือที่แนะนำ

1. **G1** (ชื่อฟอร์ม) — เสี่ยงต่ำ ไม่มี dependency เห็นผลทันที เอาไว้อุ่นเครื่อง
2. **G3** (rubric) — แก้ไฟล์เดียว (`RatingCard.tsx`) เสี่ยงต่ำ
3. **เฟสเตรียม** — เปลี่ยนเลข step เป็นค่าคงที่แบบมีชื่อ **ทั้ง `EvaluationWizard` และ `AdvisorWizard`** ก่อนจะแทรก/สลับส่วนใด ๆ (กัน off-by-one ทั้งชุด — สองฟอร์มนี้ขยับพร้อมกัน 4–5 ส่วน)
4. **G5** (ย้าย+แปลงส่วนกระบวนการ) — ทำก่อน G2 เพราะไม่มีคอมโพเนนต์ใหม่ และยกโมดูลข้อความกลางขึ้นมาได้เลย
5. **G4** (สเกลรายงาน 2 ฟอร์ม) — แยก `copy.rating` ออกเป็น 2 ตัวก่อน แล้วค่อยแก้ schema ทั้งสองฝั่ง แล้วค่อย migrate ข้อมูล
6. **G9** (จัดโครงสร้างอาจารย์นิเทศ) — ทำหลัง G4 เพราะส่วนรายงานที่ย้ายต้องเป็น 4 ระดับแล้ว
7. **G2** (ส่วนทักษะ 16 ข้อ) — มีคอมโพเนนต์ใหม่ + prop ใหม่ของ `RatingCard` ทำตอนโครงสร้างนิ่งแล้ว
8. **G6** (ประกอบโครงสร้าง 6 ส่วนฝั่งสถานประกอบการให้ครบ) — ปิดงานโครงสร้างฟอร์มเดิม
9. **G10** (ฟอร์มนักศึกษา) — **งานใหญ่ที่สุดของ phase** เป็นฟอร์มใหม่ทั้งฟอร์ม ทำหลังสุดเพื่อนำทุกอย่างที่นิ่งแล้วมาใช้ซ้ำ (`RatingCard` + `disabledValues`, `coop-center-copy`, แพตเทิร์น draft/step constants)
10. อัปเดต `CONTEXT.md` ให้ตรงความจริงใหม่ทั้งหมด (สเกล 4 ระดับไม่มีข้อยกเว้น, คำศัพท์ชื่อฟอร์มทั้ง 3, โครงสร้าง 6 ส่วนของทุกฟอร์ม)
11. **G8** (คู่มือ) — เขียนข้อความคู่ขนานได้ตั้งแต่ต้น แต่ **ถ่ายภาพหน้าจอหลังข้อ 10 เท่านั้น**

---

## กติกาโปรเจกต์ที่ห้ามพังระหว่างทำงานนี้

อ้างอิงจาก `web/CLAUDE.md` และ `CONTEXT.md`:

1. **ห้าม gate การเปลี่ยนหน้าด้วย validation** — ผู้ตรวจต้องเปิดดูทุกส่วนได้โดยไม่ต้องกรอกอะไร validate เฉพาะตอน submit จริง กติกานี้เคยพังแล้วครั้งหนึ่งและถูกแก้กลับมา
2. **แยกภาษาเด็ดขาด** — โหมด `th` ห้ามมีอักษรละติน โหมด `en` ห้ามมีอักษรไทย รวมถึงข้อความที่ดึงจาก DB
3. **สีแบรนด์เดียว (navy)** — ห้ามเพิ่มสีใหม่ให้คอมโพเนนต์ใหม่ ใช้ token ที่มีอยู่
4. **ใช้ `RatingCard` ไม่ใช่ `RatingScale`** — `RatingScale` เก็บไว้เพื่อ backward compatibility เท่านั้น
5. **ฟอร์มเป็น uncontrolled + มี draft restore** — ทุกคอมโพเนนต์ที่มี state ต้อง sync จาก DOM ผ่าน `formVersion` ไม่งั้นกู้ร่างแล้วค่าไม่ขึ้น
